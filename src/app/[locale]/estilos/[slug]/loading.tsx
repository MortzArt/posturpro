import { CatalogPageSkeleton } from "@/components/catalog/catalog-skeleton";

/** Route-level loading skeleton for a style detail page (T3 UX). */
export default function Loading() {
  return <CatalogPageSkeleton />;
}
