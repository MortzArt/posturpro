import { PdpSkeleton } from "@/components/product/pdp-skeleton";

/** Route-level loading skeleton for /producto/[slug] (T4 UX loading state). */
export default function Loading() {
  return <PdpSkeleton />;
}
