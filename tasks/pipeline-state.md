# Pipeline State
Task: T9 — Transactional emails
Tier: standard
Stage: COMPLETE — T9 checked off in BUILD_PLAN.md
Agent: none (pipeline done)
Last Updated: 2026-07-14
Notes: POST-QA ORTHOGONALITY CHECK (orchestrator-launched bisect agent) RESOLVED the QA e2e discrepancy: the prod-build order-placing e2e failure is PRE-EXISTING/ENVIRONMENTAL, NOT a T9 regression — and NOT a Server-Action/FormData bug either (QA's mechanism diagnosis was wrong; their conclusion was right). PROOF: checkout.spec 24/24 PASS on prod build at BOTH HEAD and pre-T9 c77b2d3 when seeded BEFORE build; identical 16/24 failure (8 order-placing tests, URL stuck at /checkout, 0 orders) at BOTH commits when db reset+seed runs AFTER build with server running. ROOT CAUSE (proven with data): supabase db reset + reseed mints fresh product/variant UUIDs (seed upserts on slug/SKU but reset wipes rows); the prod Next server serves cached page data embedding the OLD variant UUIDs (verified: served milano variant IDs ≠ DB IDs at both commits) → add-to-cart stores a dead variant id → create_order rejects → no order, no redirect. Dev servers render fresh per request, which is why dev passes. T8 Verify passed because its build ran after its final seed. NO new BUILD_PLAN ticket needed — this is a harness sequencing rule, codified below. T9 STATUS: QA PASS high confidence stands; AC 20/20; suites unit 1281/1281, integration 180/180, cart e2e 46/46 prod, checkout 24/24 prod (correct sequencing). LIVE-SEND verification BLOCKED-ON-USER (EMAIL_API_KEY etc. — see dev-done.md).
=== S5 (ultraqa) COMPLETE (prior) — VERDICT: PASS for T9, confidence HIGH. tasks/qa-report.md written (T9). NO production code changed (test-only). NEW TESTS (+26 cases, 4 files): dispatch.test.ts (+timeout-branch via fake timers, +throw-vs-reject discrimination, +claim-error path, +locale end-to-end en/es-MX customer URL & owner-stays-es-MX); templates.test.ts (+injection sweep: variant label / cancel reason / voucher reference+verification code escaped); process-payment.test.ts (+dispatch resolve-{ok:false} still 200+finalized); email.integration.test.ts (+full transition_kind matrix paid/payment_pending/payment_failed/preparing/shipped/delivered/cancelled/payment_authorized/refunded + regression_blocked & order_not_found kind=noop contract + CONCURRENT claim_email_send race → exactly one 'new'). AC MATRIX 20/20 PASS, EDGES 7/7 PASS, S4 M-1 REGRESSION-LOCKED (layout.test breakout payload). SUITES: unit 1281/1281 (68 files, was 1271), integration 180/180 (13 files, was 168) via scripts/run-integration.sh, tsc 0, eslint clean, migrations 0001..0010 apply clean. E2E: cart 46/46 PASS on PROD build. CHECKOUT+PAYMENT ORDER-PLACING E2E = BLOCKED (ENVIRONMENTAL, NOT T9): on prod build (both NEXT_QA_DIST_DIR and default .next) the placeOrder Server Action → confirmation redirect fails (no order created, client nav ERR_ABORTED, empty-FormData race — React19/Next16.2.9); PROVEN NOT T9 because checkout.spec 24/24 PASS on DEV server with T9 code AND dev log shows the live [email] PREVIEW order_confirmation firing end-to-end (AC-14 verified live). Same failure hits T7 checkout core + reproduces with default dist dir → pre-existing T7/harness issue. RECOMMEND: separate ticket for prod-build Server-Action e2e reliability (independent of T9); does NOT block T9. CLEANUP DONE: DB pristine-seeded (0 orders, 0 email_sends), .next-qa + test-results removed, tsconfig unchanged, no stray servers. Git working tree = only the 4 modified test files (orchestrator commits). DO NOT commit here.
=== S4 (ultrareviewfix) COMPLETE — verdict APPROVE (9/10). tasks/review-findings.md written; dev-done.md updated with fixes section. FINDINGS: 0 critical, 1 MAJOR FIXED (M-1: renderButton interpolated href unescaped into the anchor attribute — provider-sourced URLs (MP voucherUrl, carrier trackingUrl) could break out with a quote and inject markup into a customer inbox; fixed with escapeHtml(href) + new src/lib/email/layout.test.ts with 3 regression tests incl. explicit breakout payload), 2 minor SKIPPED justified (m-1 cosmetic comment says SITE_ORIGIN vs NEXT_PUBLIC_SITE_ORIGIN; m-2 renderParagraph escape-bypass branch has no live caller), 1 nit SKIPPED (stale header comment in run-integration.sh, non-T9 file). CRASH-BETWEEN-CLAIM-AND-SEND: at-most-once BY DESIGN — confirmed/documented/justified (migration 0010 lines 316-320 + dev-done.md + ticket edge 2): claim is race-safe insert-on-conflict-do-nothing; crash after claim leaves sent_at NULL and redelivery returns 'duplicate' (email ≠ money; ledger row enables future manual/queue retry without double-send). No timeout leak (Promise.race handles loser, timer cleared in finally). VERIFIED AIRTIGHT: webhook route zero email code; email failures never change ProcessResult/CheckoutFormState/HTTP status (isolation test proves 200 even when send throws); 0010 preserves every T8 advance_order_status reason/result contract; create_order body byte-for-byte 0008 plus locale; i18n keys symmetric 50/50; all user input HTML-escaped; EMAIL_API_KEY server-only; RPC types are type aliases (T8 gotcha honored). SUITES POST-FIX: tsc 0, eslint clean, unit 1271/1271 (68 files, +3), integration 168/168 via scripts/run-integration.sh, supabase db reset 0001..0010 clean. DB pristine (0 orders, 0 email_sends), tsconfig unchanged, no stray servers.
=== S3 (ultradev) COMPLETE (prior) — tasks/dev-done.md written. VERIFICATION (dev-run): tsc 0 errors, eslint clean, next build exit 0, migration 0010 applies clean on supabase db reset, unit 1268/1268 (baseline 1206 +62), integration 168/168 (baseline 158 +10). DB pristine (0 orders, 0 email_sends), tsconfig unchanged, no stray servers. MIGRATION 0010 (0010_email_transitions.sql, LOCAL-only, idempotent): TD-2 transition_kind derived inside advance_order_status from (from_status, to_status, payment_status, payment-only) — returned in RPC jsonb AND written to order_status_history.transition_kind; orders.locale (CHECK-constrained, immutable post-creation); email_sends ledger unique(order_id, email_kind, dedupe_key) + claim_email_send/finalize_email_send RPCs (SECURITY DEFINER, empty search_path, explicit service_role grant); create_order persists locale. SUBSYSTEM src/lib/email/: Resend behind one-module provider.ts (server-only, dev-preview short-circuit via EMAIL_DEV_PREVIEW=1, swap = one function); pure render/layout/brand (600px table, inline styles, plain-text alt, MXN via money.ts, all input escaped); 8 templates (6 customer es-MX+en, 2 owner/relay single-locale es-MX); dispatch.ts claim→render→send→finalize, failure-isolated, bounded timeout. TRIGGERS: checkout action fires order_confirmation + new_order_owner on success (isolated, non-blocking, persists locale); process-payment.ts branches on transition_kind after successful advance to fire payment_received/voucher_instructions (webhook route stays email-free). shipped/cancelled/refund_issued (T12) + contact_relay (T13) built + tested as documented seams. ENV VARS (live-send BLOCKED-ON-USER, in dev-done.md): EMAIL_API_KEY (secret), EMAIL_FROM_ADDRESS, EMAIL_OWNER_ADDRESS, NEXT_PUBLIC_SITE_ORIGIN; dev EMAIL_DEV_PREVIEW=1. DEVIATIONS (in dev-done.md): reused T8 extractVoucher via pure adapter (DRY); added finalize_email_send + NEXT_PUBLIC_SITE_ORIGIN; CreateOrderPayload.locale now required. Key files: supabase/migrations/0010_email_transitions.sql, src/lib/email/*, src/lib/payments/process-payment.ts, src/app/[locale]/checkout/actions.ts.
=== S1 (ultraplanner-research) COMPLETE (prior) — tasks/next-ticket.md + tasks/research-report.md written. Complexity: MEDIUM (~12-15 files, one small migration, new src/lib/email/ subsystem on established seams). Feature Type: LOGIC-ONLY — emails have an inbox surface but no in-app UI; S2 UI Design SKIPPED per Feature Type rules; email visual constraints carried into S3 Dev as hard requirements (600px table layout, inline styles only, neutral tokens in one brand.ts, plain-text alt part, MXN via money.ts). PROVIDER: Resend (thin SDK, vi.mock like MP tests, single server-only EMAIL_API_KEY, Node-runtime fit); Postmark documented fallback; provider.ts boundary makes swap one-file. TD-2 DESIGN: transition_kind (fixed text enum) derived INSIDE advance_order_status from (from_status, to_status, payment_status, p_order_status IS NULL) — returned in RPC jsonb AND written to new order_status_history.transition_kind column; emails branch on it, never on free-text note. IDEMPOTENCY: durable email_sends ledger unique(order_id, email_kind, dedupe_key) + claim_email_send RPC (insert-on-conflict-do-nothing → 'new'/'duplicate'), mirroring record_payment_event spine; dedupe_key = mp_payment_id for payment-linked emails, '' for one-per-order; dispatch failure-isolated (send error caught/logged, never changes ProcessResult/CheckoutFormState; triggers fire from process-payment.ts after successful advance, webhook route stays email-free). MIGRATION 0010 also REQUIRED to add orders.locale persisted at checkout (orders has NO locale column, verified vs 0003; webhook is server-to-server — reads locale via getTranslations({locale})). VOUCHER BOUNDARY: T8 does not persist OXXO/SPEI voucher fields (re-fetches from MP on confirmation page) — T9 sends voucher email only where data is present at trigger, documents the gap, does NOT add voucher-persistence schema. STATE CORRECTION: .env.local DOES contain the three MERCADOPAGO_* keys (placeholders) — earlier "no MP creds" note was wrong; no EMAIL_* vars exist, which is what matters for T9.

=== STANDING GATES (carry forward — do not drop) ===
- HUMAN-REVIEW GATE (BUILD_PLAN rule 3): T7 and T8 both have advisory SHIP verdicts but remain UNCHECKED in BUILD_PLAN.md until the user manually reviews. Do not check them off.
- T7 human-review focus: 0008_checkout.sql create_order SECURITY DEFINER fn; checkout server-action trust boundary (actions.ts); rate-limit.ts best-effort caveat + CHECKOUT_RATE_LIMIT_DISABLED unset in real deploys; money math (order.ts, discount.ts, cart/shipping.ts).
- T8 human-review focus (from ship-decision.md): webhook signature verification, amount reconciliation, refund execution, RPC transition/idempotency, secret handling.
- T8 LIVE-SANDBOX verification BLOCKED-ON-USER (only placeholder MERCADOPAGO_* keys in .env.local; all tests mock MP).
- T9 rule-3 addendum: T9 modified the checkout action (src/app/[locale]/checkout/actions.ts — locale persistence + email triggers) and src/lib/payments/process-payment.ts (transition_kind email dispatch) — include these diffs (bdd37bc, 6c19265) in the same human review pass as T7/T8. T9 itself is checked off (QA gate passed); the review flag is about the touched checkout/payment files.
- Clean-code A4 addendum (2026-07-14): the A4 refactor commit also touches the flagged checkout files — pure form-parsing helpers extracted verbatim from actions.ts to src/lib/checkout/form-parsing.ts, and both local clientIp copies replaced by the canonical src/lib/request/client-ip.ts after a proven behavior-identical diff (closes SEC-M-1, zero rate-limit keying change). Include the A4 commit in the human-review pass; verified by checkout e2e 24/24 on prod build.
- T9 LIVE-SEND verification BLOCKED-ON-USER: no EMAIL_* vars in .env.local; all tests mock the provider. Env vars documented in dev-done.md: EMAIL_API_KEY (secret, Resend), EMAIL_FROM_ADDRESS, EMAIL_OWNER_ADDRESS, NEXT_PUBLIC_SITE_ORIGIN; dev preview via EMAIL_DEV_PREVIEW=1.

=== T9 SEAMS FOR LATER TASKS ===
- T12 (admin): shipped / cancelled / refund_issued templates + send functions exist, tested, NOT live-wired — wire from admin actions. refund.ts still called nowhere until T12 auth-gates it.
- T13 (contact page): contact_relay template + send function exist, tested, NOT live-wired — wire from the contact form.
- Emails branch on order_status_history.transition_kind (RPC-returned) — never string-match note text.

=== T8 FACTS T9 BUILDS ON ===
- advance_order_status RPC (0009) is the ONLY status-transition path; writes order_status_history; payment-only mode (p_order_status=null) sets payment_status=refunded with order_status untouched (from==to in history — hence TD-2).
- Webhook processing: record_payment_event claim-then-finalize spine keyed (mp_payment_id, mp_status); callers branch on result.reason (advanced/payment_updated/noop_same_status = success).
- payment_refunds ledger + record_refund RPC exist; refund.ts is server-only, called nowhere until T12.
- Confirmation page addressed by orders.confirmation_token at /[locale]/checkout/confirmacion/[token]; confirmationPath(token) in config.
- Integer cents everywhere; currency MXN; i18n es-MX + en both locales symmetric.

=== E2E/ENV INFRA (binding) ===
- LOCAL Docker Supabase only; remote empty/unlinked — apply new migrations locally, never remote push.
- Authoritative e2e: PRODUCTION build (next build + next start), separate NEXT_QA_DIST_DIR, CHECKOUT_RATE_LIMIT_DISABLED=1. Reseed between order-placing suites; leave DB pristine. After any prod build with NEXT_QA_DIST_DIR: `git checkout -- tsconfig.json`.
- SEQUENCING RULE (root-caused 2026-07-14, bisect after T9 QA): on PROD builds, ALWAYS `supabase db reset` + seed BEFORE `next build`; after ANY reseed, REBUILD (server restart alone is unreliable — stale dist-dir cache can persist). Why: db reset mints fresh product/variant UUIDs (seed upserts on slug/SKU but reset wipes rows) while the prod server serves cached page data embedding the OLD variant UUIDs → add-to-cart stores a dead variant id → create_order rejects → order-placing e2e fails 8/24 with URL stuck at /checkout and 0 orders. Dev servers render per-request, so dev passes — do NOT mistake this signature for a Server-Action/FormData bug (that diagnosis was disproven). "Reseed between order-placing suites" therefore means reseed+rebuild on prod, or restore stock without reset.
- Test baseline after T9: unit 1281/1281 (68 files), integration 180/180 (13 files), e2e payment 8/8 + checkout 24/24 + cart 46/46 (prod build, correct seed-before-build sequencing). Migrations 0001..0010.
