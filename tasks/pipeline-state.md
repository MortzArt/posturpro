# Pipeline State
Task: T16 — B2B landing page (offices, quote form)
Tier: standard
Stage: 4
Agent: ultrareviewfix
Last Updated: 2026-08-02 17:10
Notes: S3 (Dev, ultradev) COMPLETE. Full inventory + AC map in tasks/dev-done.md. GATES GREEN: tsc --noEmit clean (whole project); eslint clean (all 23 touched files); full unit suite 1920/1920 (112 files, was 1746/108 → +174 across 4 new test files: quote submit-guard, quote rate-limit incl. contact-isolation, quote-relay template, empresas actions branch-map). e2e/empresas-quote.spec.ts 20/20 PASS (chromium+mobile: both-locale render, no-overflow 375/768, hero anchors, labeled fields + native <select> + off-screen honeypot, validation, team-size preserved on error re-render, default error-on-submit, nav+footer zero-dead-links). LIVE spot-check on :3000: /empresas + /en/empresas both HTTP 200 with pillars/process/quote-form/anchors/honeypot/select/hero-image; metadata per-locale; nav+footer /empresas links present w/ localized labels (Empresas / For business); /zz/empresas → in-shell 404 not 500 (edge 6).
  * NEW FILES (13): src/lib/config/quote.ts (caps + QUOTE_TEAM_SIZES enum + isQuoteTeamSize guard + limiter tunables + QUOTE_SUCCESS_FEEDBACK_MS); src/lib/quote/{submit-guard,rate-limit}.ts (+ .test.ts ×2); src/lib/email/templates/quote-relay.ts (+ .test.ts) — NEW pure renderQuoteRelay (all 6 fields, es-MX owner-facing, needs escaped verbatim, es-MX team-size labels); src/components/b2b/b2b-sections.tsx (B2BPillars + B2BProcess, server, IconSvgElement props, .stagger, no hover motion); src/app/[locale]/empresas/{page,quote-form,quote-form-state,actions}.tsx/ts (+ actions.test.ts); e2e/empresas-quote.spec.ts. MODIFIED (10): config.ts barrel (+quote re-export), config/imagery.ts (+B2B_HERO_IMAGE), email/dispatch.ts (+sendQuoteRelay seam, mirrors sendContactRelay), components/home/hero.tsx (+optional fallbackIcon prop, default Chair01Icon — homepage unchanged), layout/nav-items.ts (+offices, union extended) + nav-items.test.ts, layout/site-footer.tsx (offices first in STORE_LINKS), messages/{es-MX,en}.json (+empresas namespace 62 keys + nav.items.offices + footer.links.offices, lockstep, parity 0-asymmetry) + keys-used.test.ts, playwright.config.ts (+QUOTE_RATE_LIMIT_DISABLED=1).
  * KEY IMPL DECISIONS: (1) Team-size <select> is UNCONTROLLED w/ key={`teamSize-${submissionId}`} — React 19's post-action form reset drops an uncontrolled select back to placeholder on a failure re-render; re-keying remounts it with the preserved defaultValue so the chosen range survives (verified live + e2e). Text inputs preserve natively; only the select needed this. (2) UI spec's IconSvgObject type is wrong — the real @hugeicons/react export is IconSvgElement (used for pillar icons + hero fallbackIcon). (3) B2B_HERO_IMAGE FILLED with a verified EFFYDESK Unsplash office-workspace photo (photo-1688578735997-32626d2babd4, ElELSfycRvw, woman in ergonomic chair at desk) — 4/3 1400×1050 261KB, appended to public/images/SOURCES.md; still degrades to Building06Icon blank tile when null. (4) Success-path live spot-check via EMAIL_* dev env is BLOCKED-ON-USER: .env.local has NO EMAIL_API_KEY/FROM/OWNER, and Next blocks a 2nd dev server on :3000; the relay-ok→success mapping is proven exhaustively at the action level (empresas/actions.test.ts) and the sendQuoteRelay seam is byte-for-byte the shipped sendContactRelay pattern (owner-guard→render→sendWithTimeout→replyTo=visitor, EMAIL_DEV_PREVIEW path). Default e2e correctly asserts error-on-submit (edge 3), exactly as T13 QA did.
  * FIREWALL held: no file under src/app/admin/, src/components/admin/, src/components/ui/*, no :root/sans edit. No migration, no new deps.
  * Next: S4 ReviewFix (ultrareviewfix) — standard tier. Adversarial line-by-line on the 23 touched files; apply review-animations STANDARDS.md to .enter-fade/.stagger reuse; verify no fabricated proof (AC-3), enum server-boundary (edge 1), limiter isolation (edge 4), header-injection strip (edge 5). Then S5 QA is the quality gate.

=== T16 S2 (UI Design) NOTES — kept for reference ===
Notes: S2 (UI Design, ultradesign) COMPLETE → tasks/ui-design.md OVERWRITTEN with the T16 spec (prior T15 spec is in git). EXTENSION MODE (impeccable new-work §3): the page is a new WHOLE SURFACE inside the committed Casa de Azulejo world — NO concept roll, NO DESIGN.md rewrite; visual system fixed, only new purpose/hierarchy/states/form resolved. Persuade mode.
  * SECTION FLOW (5): §1 Hero (reuse `Hero`, cobalt cartouche + roman-caps headline + `Solicitar cotización` CTA→#cotizacion + `¿Cómo funciona?`→#como-funciona) → §2 Value 3-pillars (NEW `B2BPillars`) → §3 Process how-it-works numbered strip id=como-funciona (NEW `B2BProcess`) → §4 Brand breadth (reuse `FeaturedBrands`, LIVE seeded brands, omitted if empty) → §5 Quote form id=cotizacion (NEW `QuoteForm`). Order = hook→belief→objection→action. ZERO fabricated proof (AC-3).
  * NEW UI (only 2 files): src/components/b2b/b2b-sections.tsx (`B2BPillars`+`B2BProcess`, server, RSC-resolved strings + hugeicon per item, fixed 3-tuples, .stagger, NO hover motion) ; src/app/[locale]/empresas/quote-form.tsx (CLONE contact-form.tsx grammar verbatim + inline `SelectField`). Everything else composed from existing blocks.
  * FORM LAYOUT DECISION: max-w-xl container (like contact); the ONE divergence = pair the 4 short fields into sm:grid-cols-2 rows (company|name, email|phone); teamSize + needs full-width. 6 fields + honeypot(company_url). Native <select> over QUOTE_TEAM_SIZES (options mapped from empresas.form.teamSize.options.{value}; server membership-check is the boundary). Char counter on needs ONLY. Full state matrix mirrors contact; honeypot filled→FAKE success; first-invalid focus walk company→name→email→phone→teamSize→needs.
  * IMAGE SLOT: ONE new — B2B_HERO_IMAGE (string|null) in src/lib/config/imagery.ts, 4/3, licensed office-workspace stock OR null. Null-degrade uses Building06Icon (not chair) so B2B fallback reads "offices". Zero CLS. §2/§3 use line-glyph seals; §4 uses BrandLogo. May ship real photo OR null for launch.
  * NAV LABEL: "Empresas" / "For business". nav-items.ts — extend closed key union with "offices"; append {key:"offices",href:"/empresas"} AFTER contact (last).
  * FOOTER: grid already lg:grid-cols-4 fully occupied — DO NOT add a 5th column. Add offices as NEW FIRST entry in STORE_LINKS (before about). footer.links.offices = "Empresas"/"For business".
  * COPY: full es-MX + en launch-grade inventory for the `empresas` namespace written verbatim in ui-design.md §Copy Inventory (NO `contacto` namespace exists — Spanish /contacto reads `contact`; /empresas gets its own `empresas` namespace). All new keys → both message files in lockstep + keys-used.test.ts CONSUMED_KEYS.
  * PAGE RSC (empresas/page.tsx): mirror contacto/page.tsx (resolveLocale, generateMetadata from empresas.metadata title+description both locales, build flat QuoteFormLabels bag, readB2BBrands try/catch→[] clone of homepage readFeaturedBrands). NO notFound() gate — copy-driven, renders with empty tables (edge 6).
  * MOTION (Emil, reused/RM-gated): .enter-fade section mounts, .stagger tile cascade (40ms step cap 5), .link-arrow. No new motion invented.
  * Next: S3 Dev (ultradev) — implement per ui-design.md + the S1 REUSE MAP below. Standard tier.

--- (prior S1 note preserved) ---
S1 (PlanResearch, ultraplanner-research) COMPLETE. tasks/next-ticket.md + tasks/research-report.md written for T16. Complexity=MEDIUM, Feature Type=full-feature. Recommended slug=/empresas (both locales; NO RESERVED_SLUGS/[pageSlug]/[...rest] collision — static segment wins). Email decision: NEW pure renderQuoteRelay template + NEW sendQuoteRelay dispatch seam (contact-relay hardcodes name/email/subject/message; quote has company/contact/email/phone/teamSize/needs + different subject). Rate limit: SEPARATE createSlidingWindowLimiter INSTANCE (own key-space) so quote never shares a bucket with contact. Page is COPY-DRIVEN from a new `empresas` i18n namespace (like the homepage), NOT a static_pages DB row — no migration, renders with empty tables. Team size = constrained enum QUOTE_TEAM_SIZES single-sourced for the <select> + the server guard membership check (the one genuinely-new validation). No new deps, no migration, no admin/ui-primitive edits (firewall holds).
  * REUSE MAP (verbatim clone targets): action=contacto/actions.ts; client=contacto/contact-form.tsx (add labeled native <select> for team size, else same fieldClasses/honeypot/state-matrix); state=contact-form-state.ts; guard=lib/contact/submit-guard.ts (+ QUOTE_TEAM_SIZES check); limiter=lib/contact/rate-limit.ts; config=lib/config/contact.ts; template=lib/email/templates/contact-relay.ts (NEW quote-relay.ts); page shell + generateMetadata = contacto/page.tsx + homepage page.tsx.
  * COMPOSE (T15 world): src/components/home/{hero,editorial-band,section-header}.tsx (props+null-degrade in report); image slots imagery.ts string|null; motion globals.css .enter-fade(414)/.link-arrow(468)/.card-lift(492)/.stagger(530); cobalt cartouche + caption-bar-as-AA-scrim (EditorialBand grammar).
  * WIRING: nav-items.ts NAV_ITEMS + closed key union → add {key:"offices",href:"/empresas"} (flows to site-header desktop + mobile-nav drawer auto); site-footer.tsx add offices link; both msg files empresas.* + nav.items.offices + footer.links.offices in lockstep; keys-used.test.ts CONSUMED_KEYS update.
  * TEST: e2e webServer=npm run dev (playwright.config.ts) with env hatch pattern; success path exercisable via EMAIL_OWNER_ADDRESS=dummy + EMAIL_DEV_PREVIEW=1 + QUOTE_RATE_LIMIT_DISABLED=1; default e2e asserts error-on-submit (owner addr unset). 4 new unit test files + 1 e2e mirroring the contact suite.
  * S2 (ultradesign) SCOPE: this page lives INSIDE the committed Casa de Azulejo world (DESIGN.md) — Persuade mode. Do NOT run a fresh new-work direction roll; the world is decided. Design the section skeleton (hero pitch → why-PosturPro-for-offices [3 pillars] → how-it-works/quote process → optional brand-breadth strip via REAL seeded brands → quote form), the quote-form layout + team-size <select> treatment, and any image slot, all in cobalt/grout/roman-caps/cartouche + shipped motion. HONEST COPY ONLY — zero fabricated proof. Write tasks/ui-design.md.

=== T16 S1 (PlanResearch) COMPLETE — prior T15 notes below are the world reference ===

=== T15 COMPLETE — QA PASS (Stage S5, ultraqa, standard tier, confidence HIGH, 2026-08-02) ===
- STANDARD PIPELINE COMPLETE: PlanResearch → UI Design (impeccable new-work) → Dev (7329b78) → ReviewFix (77a7781, APPROVE 9/10) → QA PASS (this stage). QA is the quality gate for the standard tier (no verify stage) → T15 SHIPS. Marked [x] in BUILD_PLAN.
- QA VERDICT: PASS (the reskin broke nothing — ui-only scope), confidence HIGH. AC 19/19 covered+passing, edges 9/9 handled. ZERO T15-caused bugs. Report: tasks/qa-report.md (overwrote T13).
- NEW TESTS (+20): 17 unit (4 new files) + 3 e2e (1 new firewall smoke spec). Files: src/components/home/editorial-band.test.tsx (7 — filled/null slot, editorial-band-fallback degrade, aspect box reserved, .enter-fade RM-gated, bg-primary AA scrim edge5); src/components/catalog/catalog-banner.test.tsx (4 — filled/null, catalog-banner-fallback degrade, 21/9 aspect reserved); src/components/layout/direction-contract.test.tsx (3 — REAL HTML comment node, seed=d43cafe8 + all 5 blocks + FINISH, hidden+aria-hidden zero footprint, AC-2); src/lib/config/imagery.test.ts (3 — string|null contract, non-empty local /public path, no remote host AC-10); e2e/theme-firewall.spec.ts (3 — storefront body .theme-storefront + d43cafe8 both locales; admin login body has NEITHER — the durable DOM-signal-only firewall regression test, survives palette tweaks).
- FULL SUITE (independently run, not trusted): unit 1746/1746 (108 files, was 1729/104 → +17,+4); integration 253/253 (23 files, fresh reseed); tsc --noEmit clean; eslint clean (all new/touched); prod build exit 0 (all 11 storefront + all admin routes present). Warm-prod e2e: 104 storefront + 3 firewall PASS; 11 taxonomy-detail "failures" = BUG-1 below (pre-existing, non-T15).
- FIREWALL PROVEN 3 WAYS (the AC the task hinged on): (1) live e2e — admin login body carries NO .theme-storefront, storefront does on both locales; (2) built output — d43cafe8 direction contract in 120 prerendered storefront HTML files (es-MX.html + en.html), 0 admin files; (3) git file-audit — no src/app/admin/, src/components/admin/, or src/components/ui/* file edited across all of T15. Font seam --font-heading→--font-heading-family (defaults Inter in :root, serif under .theme-storefront, no cycle). Token grep: 0 hardcoded brand color, 0 raw amber/emerald (all 10 files promoted to --warning/--success). i18n symmetric 438/438. formatMXN/tabular-nums/compare-at line-through + honeypot off-screen + grid column-count all preserved.
- ⚠️ BUG-1 (HIGH, PRE-EXISTING, NOT T15 — T14 MUST FIX BEFORE LAUNCH): fresh prod build (npm run build && start) returns HTTP 500 on ALL taxonomy DETAIL pages — /categorias/[slug], /marcas/[slug], /estilos/[slug] (index pages, /sillas, /producto/[slug], home, cart, checkout, static, 404 all 200). Digest = DYNAMIC_SERVER_USAGE: these 3 pages are ● SSG (generateStaticParams) and await searchParams (?page) via PaginatedProductListing inside <Suspense> during prerender → throws. /sillas is ƒ Dynamic so unaffected; /producto/[slug] is ● but reads no searchParams so unaffected. PROOF it's not T15: (a) git diff e637a61..77a7781 — ZERO T15 commits touched the 3 [slug]/page.tsx, PaginatedProductListing, catalog queries, or catalog/types; (b) dev server returns 200 for all three (prod-build-only bug); (c) the only T15-touched components on these pages (category-tree, catalog-skeleton) have cosmetic-only className diffs. Dormant since T3 catalog shipped, masked because playwright webServer = npm run dev (where they're 200). LIKELY FIX (T14): export const dynamic = "force-dynamic" on the 3 [slug] pages (match /sillas's posture), OR move the searchParams await out of SSG — one-line-per-page, no reskin involvement. T14 SHOULD ALSO switch the e2e webServer to a prod build so this bug class can't hide again.
- WHAT EXISTS NOW (for T16 + T14):
  * THE CASA DE AZULEJO WORLD — cobalt (#1545a2, oklch(0.42 0.16 262)) line-and-wash on milk-white glaze (#f7fafe) ground; grout-seam borders; roman-caps Libre Caslon Text captions in cartouche frames; mustard/gold reserved inside frames; product photos framed never tinted. Light-only (dark mode decommissioned for storefront). Radius 0.375rem. All pairings WCAG AA (computed). Applied to ALL 11 storefront routes + persistent chrome.
  * DESIGN.md (repo root, 23 KB) = THE DESIGN AUTHORITY — palette + AA contrast table, type + es-MX glyph coverage + --font-heading binding, spacing/radius/elevation, imagery art direction + placeholder posture (no fabricated proof), per-surface rules, admin firewall mechanism, dark-mode decommission, motion (Emil, unchanged). T16 (B2B landing) MUST follow this world and read DESIGN.md first.
  * IMAGE-SLOT SYSTEM (how to swap real photos): config-driven string|null slots. HERO_IMAGE + SHOWROOM_MAP_IMAGE in src/lib/config/static-pages.ts; EDITORIAL_BAND_IMAGE + CATALOG_BANNER_IMAGE in src/lib/config/imagery.ts (re-exported from src/lib/config.ts). Every slot: non-null = /public path rendered via next/image in a cobalt cartouche (aspect box reserved); null = blank cobalt tile + chair glyph (never broken <img>, zero CLS). SWAP A REAL PHOTO: drop the file at the same /public path OR point the config const at a new /public path — no layout rework. All 3 current assets are licensed Unsplash (≤300 KB, SOURCES.md provenance). Showroom map stays null (no real address). NEVER point a slot at fabricated-proof imagery (PRODUCT.md hard rule).
  * ADMIN-FIREWALL PATTERN (reusable): admin is a PARALLEL root layout (src/app/admin/layout.tsx, NOT under [locale]); both bodies share bg-background/font-sans/:root tokens. The reskin lives entirely in a .theme-storefront class holding all cobalt tokens + --radius + semantic/gold/whatsapp + --font-heading-family override, applied ONLY on the storefront <body> ([locale]/layout.tsx:78). Admin never gets the class → resolves the untouched neutral :root. Shared ui/* primitives are token-driven and NOT restyled. @theme inline registers success/warning/gold/whatsapp colors additively (undefined under admin scope). This is THE pattern for any future storefront-only theming — never swap :root or the shared sans export.
- MIGRATIONS: none (0013 still latest; next 0014). DEPS: none new (second next/font/google family only). next.config.ts UNCHANGED (all assets local /public).
- Next: pipeline idle. Continuous Improvement Mode or the next BUILD_PLAN task. Recommended next per blocked-by graph: T16 (B2B landing, blocked by T15 — now unblocked) then T14 LAST (must fix BUG-1 + repoint e2e to prod build).

=== T15 S4 (ReviewFix) NOTES (kept for reference) ===
Notes: S4 (ReviewFix, ultrareviewfix) COMPLETE — APPROVE, 9/10. 0 critical, 1 major, 2 minor (1 fixed, 1 skipped-classified). M-1 (the only real defect): CATALOG_BANNER_IMAGE slot + its 294KB /public asset shipped DEAD (never rendered anywhere — DESIGN.md↔code mismatch, dead code + dead bundle weight, unmet "filled slots" owner decision, AC-8/AC-15 gap). FIX: new src/components/catalog/catalog-banner.tsx (21/9 cobalt cartouche mirroring Hero/EditorialBand null-degrade grammar; next/image w/ sizes, NO priority since not LCP; degrades to catalog-banner-fallback blank tile when null; aspect box reserved = 0 CLS) + wired into [locale]/sillas/page.tsx above the toolbar/grid gated on !active (unfiltered index only — does NOT affect the e2e grid-cols column-count assertion which selects product-grid) + new symmetric catalog.banner.imageAlt i18n key (en + es-MX lockstep). m-1 FIXED: /sillas <h1> was residual-neutral → font-heading uppercase tracking-wide (AC-6 consistency). m-2 SKIPPED (false positive): impeccable broken-image hook on catalog-banner.tsx — src binds a real /public asset, null branch is a token tile, same grammar as Hero. FIREWALL INDEPENDENTLY RE-VERIFIED: admin body has NO .theme-storefront; no admin/ or ui/* file edited; Radix portals mount into the themed storefront <body> (inherit cobalt) while admin's separate body stays neutral; font seam --font-heading-family defaults Inter in :root, overridden only under scope (no cycle). Token grep clean, i18n symmetric (527 keys), structural e2e signals green on warm server (compare-at line-through, honeypot, catalog column-count=2). FLAKY TESTS (dev-flagged): 2 catalog nav-click e2e fail COLD / pass WARM (re-run green ~5s) — pre-existing Playwright webServer:npm-run-dev vs Next on-demand-compile race, NOT the reskin (category-tree.tsx link+testid unchanged; new font is display:swap non-blocking); flag e2e-vs-dev-server setup for T14. VERIFIED AFTER FIXES: tsc clean, eslint clean, unit 1729/1729, i18n symmetric, targeted catalog+PDP e2e green (warm). Next: S5 QA (ultraqa) — standard-tier quality GATE (no verify stage). Scope: "the reskin broke nothing" (ui-only, no new logic) — assert firewall (admin unchanged), all 19 ACs, image null-degrade paths (set slots null → fallback tiles incl. the NEW catalog-banner-fallback), no-fabricated-proof, bilingual parity, structural e2e signals, full suite green. Consider running e2e against a PROD build to retire the cold-compile flakiness.

=== S3 (ultradev) COMPLETE — "Casa de Azulejo" BUILT (2026-08-02) ===
- FIREWALL (AC-11/12): globals.css — neutral :root/.dark UNTOUCHED (admin's world); NEW `.theme-storefront` block holds all cobalt tokens + --radius .375rem + semantic/gold/whatsapp/shadow tokens; applied on storefront <body> in [locale]/layout.tsx ONLY. Registered success/warning/gold/whatsapp in `@theme inline` (additive, resolve only under scope). PROVEN: admin/login screenshot BYTE-IDENTICAL before/after; admin/settings visually identical (only diff = a live Q&A count badge, not styling). Token probe: admin --radius=.625rem --primary=neutral H1=Inter; storefront --radius=.375rem --primary=cobalt H1="Libre Caslon Text". No file under src/app/admin/ or src/components/admin/ or src/components/ui/* edited.
- FONT SEAM (AC-5): fonts.ts adds headingSerif=Libre_Caslon_Text (latin+latin-ext, 400/700). sans=Inter kept (widened to latin-ext, safe for admin). `@theme` font-heading now emits var(--font-heading-family); :root defaults it to var(--font-sans) (admin/not-found → Inter); .theme-storefront overrides to the serif. NOT self-referential (renamed resolver var to avoid the @theme cycle that initially left headings on Inter — fixed). es-MX accented headings ("ergonómicas","cómo") render in the serif, no glyph fallback.
- DIRECTION CONTRACT (AC-2): JSX comments are stripped before render → created src/components/layout/direction-contract.tsx that emits a REAL HTML comment via dangerouslySetInnerHTML on a hidden wrapper; first child of storefront <body>. `d43cafe8` greppable in emitted HTML on BOTH locales.
- IMAGERY (AC-8/9/10 — owner chose FILLED slots): downloaded 3 licensed Unsplash photos (all EFFYDESK, bright cool-neutral daylight, single chair/workspace, no faces/text/proof) into public/images/{hero,editorial,catalog}, each ≤300KB at correct aspect (hero 4/3 154KB, editorial 16/9 295KB, catalog 21/9 294KB). public/images/SOURCES.md records photo id + photographer + profile per file (Unsplash License, no attribution required). NEW src/lib/config/imagery.ts (EDITORIAL_BAND_IMAGE, CATALOG_BANNER_IMAGE → real /public paths); HERO_IMAGE in static-pages.ts → real hero path. ALL slots still degrade to blank cobalt tile if set null. Showroom map stays null (no real address — Out of Scope, degrades to pin-glyph). next.config.ts UNCHANGED (local /public, no new host).
- NEW COMPONENT: src/components/home/editorial-band.tsx (AC-8) — wide cartouche lifestyle slot (16/9→21/9) + cobalt caption bar (= AA scrim 8.37:1) carrying the ergonomics CLAIM (i18n home.editorial.title/body/imageAlt, added to BOTH message files in lockstep, symmetry verified). Curation/ergonomics claim ONLY — ZERO fabricated proof. Wired into homepage between Featured Products and Featured Brands, strings pre-resolved in RSC.
- PER-SURFACE (AC-6/7): chrome (header wordmark→roman-caps cobalt, WhatsApp FAB→bg-whatsapp green, footer/mobile-nav/language-toggle), homepage (hero cobalt cartouche + roman-caps headline + real photo, section headers roman-caps), catalog (13 files), PDP (8 files), cart+checkout (13 files), static/contact/showroom/404/error (8 files). Headings use font-heading; section titles uppercase tracking-wide, product/card titles sentence-case.
- SEMANTIC PROMOTION (edge 9): all 10 storefront amber/emerald files → --warning/--success (glyph+text + AA kept). OXXO/SPEI pending = CALM bg-warning/10 border-warning/30 panel, NEVER error-red. oxxo-spei-instructions.test.tsx updated in lockstep (asserts warning class — the ONE test rename, documented). global-error.tsx UNTOUCHED (edge 8).
- STRUCTURAL SIGNALS PRESERVED (verified live-DOM): product-grid gridTemplateColumns = 4 cols; compare-at line-through present; contact honeypot left=-9999px. All data-testids preserved. formatMXN/tabular-nums untouched.
- GREEN GATES: tsc --noEmit clean (whole project); eslint clean (src/**); unit 1729/1729 (104 files — 1 flaky waitFor timeout in payment-panel navigation on first full run, PASSED on re-run + passes isolated 17/17); at-risk e2e (catalog/product-detail/static-pages-contact/home) exit 0 (2 catalog nav-click tests marked FLAKY = pass-on-retry, pre-existing dev-server cold-compile timing, NOT reskin regressions). No horizontal overflow at 375/768. Dark mode decommissioned (storefront never triggers .dark).
- FILES: 3 created (editorial-band.tsx, direction-contract.tsx, imagery.ts) + public/images/{SOURCES,README}.md + 3 image assets; ~52 modified. NO new deps, NO new migration, NO next.config.ts change.

=== S2 (ultradesign) COMPLETE — COMMITTED WORLD: "Casa de Azulejo" (2026-08-02) ===
- NEW-WORK ROLL (hard gate, ran): direction seed key d43cafe8, --scope direction --mode persuade, ASSIGNED INDEX 6 of my grounded list. My grounded #6 = "Mexican Talavera/azulejo storefront" AND the strongest dealt challenger = `architecture-places-azulejo-station-hall` CONVERGED on the same world → committed it. Fused, weighed on audience-ID + product-clarity (both won vs the white-grid category default). Telemetry ping sent (--chosen architecture-places-azulejo-station-hall). Viewed the QUALITY BAR board+hero to set craft bar.
- COMP/VISUALIZE STEP: DEFERRED to S3. generate-image.mjs needs OPENAI_API_KEY (UNSET this stage) and no harness image tool in S2 → per new-work §7 the comp render + finish review happen in S3's BUILD (S2 deliverable is DESIGN.md + spec, not a built page). S3 MUST: render hero/homepage comp, run impeccable-finish-reviewer FRESH (no forked history), then impeccable-documenter re-confirms DESIGN.md from the BUILT world.
- THE WORLD: tin-glazed azulejo — COBALT (#1545a2, oklch(0.42 0.16 262)) line-and-wash on MILK-WHITE GLAZE ground (#f7fafe, oklch(0.985 0.006 250)); grout-seam borders; roman-caps captions in cartouche frames; mustard reserved INSIDE frames only. RULE: tile world = the FRAME (chrome), product photos = the SCENE inside cartouche frames (blue never tints the product). LIGHT-ONLY (a tiled hall is lit/glazed).
- PALETTE: all pairings WCAG AA VERIFIED (computed, scratchpad-t15/contrast.mjs): fg/bg 14.10, mutedFg/bg 6.55, primaryFg/primary 8.37, secondaryFg/secondary 9.38, gold-fg/gold 5.87, destructive 5.15, success 4.95, warning 4.77 (darkened to clear AA-normal). Text-over-image ALWAYS on cobalt scrim/caption bar (8.37:1 regardless of photo luminance).
- TYPE: heading = Libre Caslon Text (painted-roman caps, latin+latin-ext for es-MX; --font-heading STOREFRONT-scoped, no longer aliases --font-sans). Body = Inter KEPT (identity carried by cobalt+roman-caps heading; zero admin-font risk, bounded bundle). --radius 0.625→0.375rem (tile-crisp).
- FIREWALL MECHANISM (AC-11/12, THE #1 risk): admin is a PARALLEL ROOT LAYOUT (src/app/admin/layout.tsx, NOT under [locale]); both bodies use identical bg-background/font-sans. SOLUTION: keep neutral :root(+.dark) AS-IS = admin's world; add `.theme-storefront` class holding ALL cobalt tokens; apply it on storefront <body> in [locale]/layout.tsx ONLY. Admin never gets the class → resolves untouched :root. --font-heading serif binds only under .theme-storefront (admin dialogs keep sans). Shared ui/* NOT restyled (token-only). Verify: screenshot /admin before/after = identical.
- SEMANTIC COLORS (edge 9): PROMOTE the 10 STOREFRONT amber/emerald files to --warning/--success tokens (glyph+text kept, AA kept). Storefront files: catalog/stock-badge, product/qa-form, checkout/{discount-code-field,payment-panel,checkout-summary,oxxo-spei-instructions}, cart/{order-summary,free-shipping-progress}, contacto/contact-form, checkout/confirmacion/[token]/page. ADMIN amber/emerald files (admin/orders/*, order-status-meta.ts) = OUT OF SCOPE (firewall).
- WHATSAPP FAB (AC-7): brand primary is now cobalt, so FAB gets dedicated --whatsapp green token (oklch(0.63 0.16 155)); whatsapp-button.tsx ~line 54 bg-primary → bg-whatsapp.
- DARK MODE: DECOMMISSIONED for storefront (azulejo is light-only). .dark block RETAINED only as admin's untouched :root world; never activated on storefront. Documented so review doesn't flag it stale.
- IMAGE SLOTS (AC-8/9): cartouche frame = universal image container. NEW config slots EDITORIAL_BAND_IMAGE + CATALOG_BANNER_IMAGE (string|null, mirror HERO_IMAGE), ALL null Phase-1 → blank cobalt tile w/ glyph (native to azulejo, intentional not broken). CREATE public/ (does not exist). NEW component src/components/home/editorial-band.tsx (ergonomics band, curation/ergonomics CLAIM only, ZERO fabricated proof). next.config.ts UNCHANGED unless owner picks remote-stock placeholders (Path B).
- E2E-SAFE (confirmed): preserve gridTemplateColumns (catalog), compare-at line-through (PDP), honeypot off-screen left (contact/mobile-filter), all data-testids, formatMXN/tabular-nums. AC-2: direction-contract HTML comment as first <body> child in [locale]/layout.tsx, seed d43cafe8, must be greppable after build.
- OWNER-VETOABLE DECISIONS (flagged in ui-design.md §10): (1) the cobalt-azulejo Mexican identity itself, (2) body face stays Inter, (3) dark mode decommissioned, (4) all editorial slots ship null (no fabricated proof) until owner provides photography.

=== S2 (ultradesign) — RUN THE IMPECCABLE NEW-WORK FLOW. Key directives: ===
- DO NOT pick the visual world from the ticket — the ticket deliberately does NOT prescribe palette/type/aesthetic. Run .claude/skills/impeccable/ new-work.md IN ORDER: run scripts/context.mjs once, then concept-seed.mjs (--scope direction --mode persuade for the storefront's Persuade surfaces) — writing artifact code before the roll is dealt+acknowledged is a contract violation. Present ONE committed direction + challengers + canon (serve-question.mjs; structured tool if headless). generate-image.mjs IS available → do the comp/visualize step. Build with full commitment, run impeccable-finish-reviewer FRESH, then impeccable-documenter writes DESIGN.md from the BUILT world (root, currently absent).
- THE #1 RISK (from S1 research): brand tokens + font + 5 ui primitives are SHARED with admin. `@/app/fonts` sans export is imported by [locale]/layout.tsx AND admin/layout.tsx AND not-found.tsx. globals.css :root tokens feed admin's bg-background/text-foreground/font-sans. src/components/ui/{button,badge,alert-dialog,dialog,tabs}.tsx imported by admin (24/8/5/4/2×); dialog+alert-dialog consume --font-heading. A naive :root/sans swap re-skins ADMIN → AC-11/12 FAIL. RECOMMENDED FIREWALL: apply new brand tokens + display font storefront-scoped under [locale]/layout.tsx (wrapper/selector), NOT the global :root or shared sans export; keep ui/* token-driven. Screenshot /admin before+after to prove untouched.
- TOKEN SEAM: ONE :root(+.dark) block globals.css:51-131, all grayscale oklch(L 0 0), --radius:0.625rem. Motion layer (globals.css:145-838, ~20 classes + --ease-* tokens) is EMIL AUTHORITY — KEEP VERBATIM (impeccable owns look, Emil owns motion). --font-heading currently ALIASES --font-sans (line 12) — a real display face needs a new binding.
- IMAGE SLOTS: pattern exists (HERO_IMAGE/SHOWROOM_MAP_IMAGE = string|null in config/static-pages.ts, degrade to token glyph panel). NO public/ dir exists yet — create it. Extend config-slot pattern for new lifestyle/editorial bands. Products flow from DB (picsum via SEED_IMAGE_BASE_URL). New hosts → next.config.ts remotePatterns. NEVER fabricate proof (testimonials/reviews/sales) — PRODUCT.md hard rule.
- TOKEN-CLEANLINESS CAVEAT: storefront ~95% token-clean for BRAND color, BUT ~12 files hardcode SEMANTIC amber(warning/OXXO-SPEI/low-stock)+emerald(success/discount/free-ship), always glyph+text AA-safe (stock-badge.tsx:43, checkout-summary.tsx:135/247/270, payment-panel.tsx:292/313/351/396, oxxo-spei-instructions.tsx:85, discount-code-field.tsx:151, cart/order-summary.tsx:108, free-shipping-progress.tsx:79, qa-form.tsx:338, contact-form.tsx:355/465). Decide once: keep as status semantics OR promote to --warning/--success; apply consistently.
- E2E IS RESKIN-SAFE: storefront specs assert NO colors/fonts/computed-styles — only visibility/text/href/structure. ONLY reskin-fragile signals are STRUCTURAL: catalog grid gridTemplateColumns, PDP compare-at line-through, contact/mobile-filter honeypot off-screen left. Preserve those + all asserted data-testids.
- FONT es-MX: ~160 accented chars (á é í ó ú ñ ¿ ¡) → chosen faces MUST cover Latin Extended-A; next/font subsets; verify a "ñ/í/¿…?" heading.
- global-error.tsx uses hardcoded hex+system-ui BY DESIGN (replaces whole doc, no tokens/providers) — intentional exception, don't token-ify, note it so review doesn't flag it.
- SCOPE: 11 storefront routes ([locale] tree) + ~40 storefront components. Admin fully isolated (NO admin route under [locale]). Bilingual parity 614/614 keys (lockstep any new copy). formatMXN/tabular-nums/integer-cents unchanged. Mobile-first, AA, prefers-reduced-motion floors binding.

=== PRIOR STAGE NOTES (T13, kept for reference) ===

=== T13 COMPLETE — QA PASS (Stage S5, ultraqa, standard tier, confidence HIGH, 2026-08-02) ===
- STANDARD PIPELINE COMPLETE: PlanResearch → UI Design → Dev (b4181e3) → ReviewFix (4fccebc, APPROVE 9.5/10) → QA PASS (this stage). QA is the quality gate for the standard tier (no verify stage) → T13 SHIPS. Marked [x] in BUILD_PLAN.
- QA VERDICT: PASS, confidence HIGH. AC 20/20 covered+passing, edges 10/10 covered+passing. ZERO product bugs. Report: tasks/qa-report.md (overwrote T12).
- NEW TESTS (+64): 29 unit + 9 integration + 26 e2e new (2 e2e fixed). Files: src/app/[locale]/contacto/actions.test.ts (16 — the S4-flagged submitContactForm action seam: full success/invalid/rate-limited/honeypot/{ok:false}/throw branch matrix + gate-ordering, deps mocked, pure guard real); src/lib/config/static-pages.test.ts (13 — slug set, RESERVED_SLUGS disjointness/collision rule, isStaticPageSlug, tunables); tests/integration/static-pages.integration.test.ts (9 — getStaticPageBySlug live: es-MX base, en overlay GENUINELY resolves distinct from base, edge1 missing→null, edge2 unpublished→anon-RLS-filtered→null + direct anon confirm, edge3 no-overlay→es-MX per-field fallback, seed idempotency 9 pages/18 translations re-run unchanged); e2e/static-pages-contact.spec.ts (19 — featured sections render, 7 generic pages 200, FAQ #anchor deep-link, /envios-y-devoluciones 404 edge10, /en overlay pages, showroom both locales map-degrades, contact form labeled+honeypot-offscreen+validation+error-on-submit edge4, all 9 footer links →200).
- FULL SUITE (independently run, not trusted): unit 1729/1729 (104 files, was 1700/102 → +29,+2); integration 253/253 (23 files via test:integration fresh reset+seed, was 244/22 → +9,+1); tsc --noEmit clean; eslint clean (all new/changed); e2e T13+regression 33/33 chromium SERIAL.
- BUGS FOUND+FIXED (2 real E2E regressions dev+reviewfix left in the shipped suite — TEST bugs vs correct new product behavior, NOT product bugs): (1) e2e/home.spec.ts referenced removed testids home-cta-catalog/home-link-brands → T13 renamed to hero-cta-catalog/hero-link-brands (FIXED + asserts CTA→/sillas, secondary→/marcas); (2) e2e/whatsapp-and-footer.spec.ts asserted footer-link-shipping→/envios-y-devoluciones → T13 split to /envios (FIXED + added footer-link-returns→/devoluciones). Also fixed 2 test-bugs in my own new spec during the run (FAQ same-doc-hash status→fresh cross-doc load w/ fragment; honeypot toBeVisible→assert hiding mechanism off-screen left<-1000/tabindex-1/aria-hidden).
- OBSERVATIONS (non-blocking, T14 cleanup): (a) persistent dev DB has a STALE `envios-y-devoluciones` static_pages row (10 rows) — DEV-DB DRIFT not a T13 bug; a fresh supabase db reset+seed yields EXACTLY 9 pages+18 translations (the seed data defines only 9; seed is upsert-only, does NOT prune unknown slugs). Orphan renders nowhere ([pageSlug] 404s non-isStaticPageSlug; footer no longer links it). Fix once: `delete from static_pages where slug='envios-y-devoluciones'` or reset. (b) Dev-server e2e cold-compile flakes under 4-worker parallelism on pre-existing catalog/cart/i18n specs — all pass serial/isolated once warm, none touch T13 files (documented reason authoritative storefront e2e uses PROD build). Not a T13 concern.
- WHAT EXISTS NOW (for T14): full storefront content layer. 9 data-backed static pages single-sourced in src/lib/config/static-pages.ts (STATIC_PAGE_SLUGS=7 generic + CONTACT_SLUG/SHOWROOM_SLUG; RESERVED_SLUGS load-time collision guard). Route shape: ONE generic src/app/[locale]/[pageSlug]/page.tsx (generateStaticParams over the 7; isStaticPageSlug guard→notFound edge10; generateMetadata locale-validated) + explicit contacto/ + showroom/ folders. getStaticPageBySlug(slug,locale) (src/lib/content/static-pages.ts): RLS public-client es-MX base + per-locale translations overlay (field title/body) w/ es-MX fallback (AC-4 real, RLS 0005 grants anon SELECT); degrade-to-null (never 500); unstable_cache per-(slug,locale) tag 'static-pages'. StaticPageBody renders parseStaticBody() output as ESCAPED React children (never dangerouslySetInnerHTML, AC-17); ## heading→<h2 id=slugified> native :target FAQ deep-links. Contact: submitContactForm server action (honeypot→fake-success / validate→invalid+preserved-values / rate-limit→rate-limited / relay ok→success-clears !ok||throw→error-raw-reason-never-shown) wiring the previously-dark T9 sendContactRelay seam; checkContactRateLimit dedicated per-IP sliding window (CONTACT_RATE_LIMIT_DISABLED=1 hatch); validateContactSubmission pure (trim→stripControlChars name/subject→caps→EMAIL_PATTERN). Showroom Option A (address/hours in body + SHOWROOM_MAP_URL/IMAGE config, both null Phase 1→token panel/omitted link). Homepage: hero (always, HERO_IMAGE null→chair-glyph panel) + FeaturedProducts (listProducts pageSize=HOME_FEATURED_PRODUCTS=8) + FeaturedBrands (listBrands slice HOME_FEATURED_BRANDS=6), sections omitted on empty (edge8), featured reads degrade to [] (edge9). Footer recolumn: shipping/returns split (/envios + /devoluciones), Legal column, showroom contextual link — ZERO dead links (AC-10). Seed: 9 static_pages + 18 en translations, idempotent. Both storefront locales symmetric (434/434 keys).
- KEY FACTS FOR T14: (1) T13 unblocks T14 (blocked by: T12,T13 — both now [x]). (2) Per-page <title> exists via generateMetadata on every static page + homepage (metadata namespace); T14 owns the REST of SEO — sitemap.xml, structured data, cookie consent, canonical/og, image-perf pass. (3) Contact SUCCESS live-send still blocked-on-user (EMAIL_* env; dev path EMAIL_OWNER_ADDRESS=dummy+EMAIL_DEV_PREVIEW=1+CONTACT_RATE_LIMIT_DISABLED=1) — action-level success mapping + dispatch relay branches unit-covered; e2e asserts correct default error-on-submit. (4) WhatsApp FAB still hidden until WHATSAPP_PHONE_E164 configured (existing guard). (5) Legal copy (aviso/terminos) is structured PLACEHOLDER with a "reference text" disclaimer — real text pending client input (Out of Scope, will need a content pass). (6) HERO_IMAGE/SHOWROOM_MAP_* null Phase 1 → token panels; set config when real assets/address land. (7) Static-page BODY content is DB translations (seeded), NOT message files; UI chrome is message namespaces (staticPages via catalog.breadcrumb reuse, contact.*, home.*, showroom.*, footer.*).
- MIGRATIONS: NO new migration (0013 still latest; next is 0014). NO new deps.

=== PRIOR STAGE NOTES (T13, kept for reference) ===
Notes: S4 (ReviewFix, ultrareviewfix) COMPLETE — APPROVE, 9.5/10. 0 critical, 0 major, 1 minor found+FIXED inline (m-1: interior CR/LF control chars in contact name/subject reaching the relay email SUBJECT line — not exploitable via Resend JSON API, but hardened defensively). FIX: new pure stripControlChars() in src/lib/contact/submit-guard.ts applied to name+subject after trim (control run → single space → trim); control-only name strips to "" → nameRequired. +2 characterization tests in submit-guard.test.ts. VERIFIED INDEPENDENTLY (not trusted): tsc --noEmit clean; eslint clean; full unit suite 1700/1700 (was 1698, +2); message parity 434/434 zero-asymmetry (flatten-diff); RLS 0005:216 confirmed grants anon SELECT on static_page translations (en overlay genuinely resolves, AC-4 real not silently-falling-back); translations unique key 0004:54 matches seed onConflict (idempotent); every AC (20/20) verified in code + every edge (10/10) handled. Contact backend mirrors checkout/Q&A abuse controls verbatim; StaticPageBody escaped-children (no dangerouslySetInnerHTML); homepage featured = bounded slices via single Promise.all (no N+1); zero dead footer/nav links; animation CSS meets STANDARDS.md. Findings in tasks/review-findings.md (overwrote T12). Next: Stage 5 QA (ultraqa) — action-level integration coverage for submitContactForm (honeypot/invalid/rate-limited/error/success mapping), the [pageSlug] notFound paths, en-overlay fallback; success path exercisable via EMAIL_OWNER_ADDRESS=dummy + EMAIL_DEV_PREVIEW=1 + CONTACT_RATE_LIMIT_DISABLED=1 (server-only import blocks the action from the default jsdom test env — QA must mock server-only or use a node-env test, as dispatch.test.ts does).

=== S4 REVIEWFIX NOTES (QA must honor) ===
- ONLY code change this stage: src/lib/contact/submit-guard.ts (added stripControlChars + applied to name/subject) + src/lib/contact/submit-guard.test.ts (+2 tests). Nothing else touched — all other S3 files reviewed and accepted as-is.
- submitContactForm action layer (actions.ts) is thin pure branch-mapping (honeypot→success / validate→invalid / rate-limit→rate-limited / relay ok→success !ok/throw→error) — NOT unit-covered at the action level yet (guard + rate-limit + dispatch branches ARE covered separately). QA should add action-level integration/e2e to close that seam.
- name/subject now guaranteed single-line clean (no CR/LF) reaching renderContactRelay subject; message still passed VERBATIM (template escapes). No behavior change to valid single-line inputs.

=== S3 DEV NOTES (reviewfix must honor) ===
Notes: S3 (Dev, ultradev) COMPLETE. 22 files created + 8 modified. Full inventory + AC map in tasks/dev-done.md. tsc --noEmit clean; eslint clean (all touched files + full src/app); unit 1698/1698 (102 files, +105 from T12's 1593 incl. 22 new contact/parse-body/rate-limit tests + expanded seed-invariants). Reseeded local DB idempotently: 9 static_pages + 18 translations. LIVE spot-check on :3000 dev server — all 20 surfaces 200 with correct h1 (10 es-MX + 10 en via translation overlay); unknown/reserved slug → in-shell 404; existing routes (/sillas /marcas /categorias /estilos) still 200 (segment precedence intact); 9 footer links all resolve 200 (ZERO dead links, AC-10); FAQ h2 ids slugified+deep-linkable; contact form + honeypot render; showroom map degrades to token panel (SHOWROOM_MAP_URL null → link omitted, address/hours always render); hero degrades to chair-glyph panel (HERO_IMAGE null, no broken img); both featured sections render (non-empty catalog). NO deviations from ui-design.md. Standard tier → next is Stage 4 ReviewFix (ultrareviewfix).

=== S3 DEV NOTES (reviewfix must honor) ===
- SLUG SPLIT committed: /envios + /devoluciones (was combined /envios-y-devoluciones). 9-slug set single-sourced src/lib/config/static-pages.ts + RESERVED_SLUGS guard + LOAD-TIME throw if a generic slug collides w/ a reserved segment. STATIC_PAGE_SLUGS (7 generic) drives generateStaticParams; contacto/showroom = own folders (excluded).
- getStaticPageBySlug(slug,locale) src/lib/content/static-pages.ts: RLS public client base es-MX row + per-locale translations overlay (field title/body) w/ es-MX fallback (AC-4); degrade-to-null (missing/unpublished/error → null → notFound, edges 1-3, NEVER throws/500); unstable_cache per-(slug,locale) tag 'static-pages' revalidate CATALOG_REVALIDATE_SECONDS; skips overlay round-trip when locale===defaultLocale.
- StaticPageBody (src/components/content/static-page-body.tsx) renders parseStaticBody() output as ESCAPED React children — NEVER dangerouslySetInnerHTML (AC-17). Parser (src/lib/content/parse-body.ts, PURE, 9 unit tests): '## ' → <h2 id=slugified(deduped)>; blank line separates <p>; intra-para newlines → <br>. slugify reused from admin/products/slug.ts. FAQ deep-link via native :target (globals.css .static-heading:target = 2px accent bar, COLOR ONLY, scroll-mt-24 on element). NO mount animation on prose (deliberate).
- CONTACT: action src/app/[locale]/contacto/actions.ts submitContactForm(prevState,formData) — honeypot→fake success (status:success no send); validateContactSubmission (src/lib/contact/submit-guard.ts, PURE, trim+caps+EMAIL_PATTERN) invalid→status:invalid+fieldErrors+RAW values preserved; checkContactRateLimit (src/lib/contact/rate-limit.ts dedicated createSlidingWindowLimiter instance, CONTACT_RATE_LIMIT_* consts, CONTACT_RATE_LIMIT_DISABLED=1 hatch) over-limit→status:rate-limited; sendContactRelay({fromName,fromEmail,subject:string|null,message}) ok→success(clears form) / !ok→log reason status:error (raw reason NEVER surfaced, AC-16) / throw→caught status:error (never throws to client). subject empty→null. Message passed VERBATIM (template escapes). State contract src/app/[locale]/contacto/contact-form-state.ts (serializable, outside "use server"). Client src/app/[locale]/contacto/contact-form.tsx copies qa-form.tsx: honeypot off-screen+aria-hidden+tabIndex-1, useActionState, full state matrix (idle/validating(focus 1st invalid)/submitting(button disabled+Enviando…)/success(role=status .enter-fade + clear via reset + focus + auto-hide CONTACT_SUCCESS_FEEDBACK_MS=6000)/error(role=alert + submit label→Retry, values preserved)/rate-limited(role=alert amber)), live char counter aria-describedby.
- CONTACT ENV: current .env.local has NO EMAIL_* → sendContactRelay returns {ok:false,'owner address unavailable'} → form shows ERROR state by default (edge 4, correct). To exercise SUCCESS in QA: set EMAIL_OWNER_ADDRESS=<dummy> AND EMAIL_DEV_PREVIEW=1 (owner-addr check runs BEFORE the preview short-circuit in dispatch.ts — both needed). sendContactRelay dev-preview/owner-unavailable/escape already unit-tested in dispatch.test.ts (green). CONTACT_RATE_LIMIT_DISABLED=1 for multi-submit tests.
- SHOWROOM (Option A): showroom/page.tsx reads showroom body via StaticPageBody (## Dirección/## Horario) + SHOWROOM_MAP_URL/SHOWROOM_MAP_IMAGE from config (both null Phase 1). grid lg:grid-cols-[1fr_minmax(0,340px)]. Null image→Location01Icon token panel (aria-hidden, no broken img); null link→"Ver en mapas" omitted; address/hours ALWAYS render (AC-18). rel=noopener target=_blank on the link.
- HOMEPAGE: page.tsx hero (always) + FeaturedProducts (listProducts pageSize=HOME_FEATURED_PRODUCTS=8) + FeaturedBrands (listBrands().slice(0,HOME_FEATURED_BRANDS=6)). Featured reads wrapped in try/catch → [] on failure so HERO survives (edge 9); section OMITTED when array empty (edge 8) — page-level guard + component-level null (belt+suspenders). Hero src/components/home/hero.tsx: HERO_IMAGE null→Chair01Icon token panel (no broken img). FeaturedBrands is async (resolves brand.logoAlt per tile); FeaturedProducts wraps ProductGrid verbatim. HomeSectionHeader shared (.link-arrow "Ver todas →").
- FOOTER RECOLUMN (AC-10): grid lg:grid-cols-4. STORE=[about] + showroom contextual link on store-info block; HELP=[faq,shipping,returns,contact]; NEW LEGAL=[warranty,privacy,terms]. footer.links.shipping label "Envíos y devoluciones"→"Envíos" + 6 new keys + footer.sections.legal. nav-items.ts UNCHANGED (/contacto verify-only, now live). ZERO dead links verified live.
- i18n: home.hero.* + home.featured.* REPLACE the old flat home.title/intro/ctaCatalog/ctaBrands (deleted losers — no other consumer). NEW contact.* + showroom.* namespaces. Both es-MX.json + en.json patched in lockstep (434 keys each, parity verified). keys-used.test.ts CONSUMED_KEYS updated. static-page BODY content is DB translations (seeded), NOT message files.
- SEED: STATIC_PAGES 4→9 in scripts/seed-data/content.ts (structured ## bodies; aviso/terminos = headed legal placeholders w/ "reference text" disclaimer; FAQ = ## question/answer). Each page ships es-MX base + en overlay. seed.ts seeds 18 translations rows (locale en, entity_type static_page, field title/body) upsert on (locale,entity_type,entity_id,field). seed-invariants-extra.test.ts asserts 9 pages, config-set parity, reserved-slug disjointness, headed legal/FAQ bodies. Reseed idempotent (verified: 9 pages/18 translations).
- NO NEW MIGRATION (0013 still latest; next 0014). NO NEW DEPS. static map = plain <img>/deep-link (no SDK).

=== S2 KEY DESIGN DECISIONS (dev must honor) ===
- SLUG DECISION (committed): SPLIT envios-y-devoluciones → /envios + /devoluciones (AC-1 lists them as 2 separate static_pages rows). 9-slug set single-sourced in NEW src/lib/config/static-pages.ts + RESERVED_SLUGS guard (sillas/marcas/categorias/estilos/carrito/checkout/producto/contacto/showroom). STATIC_PAGE_SLUGS (7 generic) asserted disjoint, drives generateStaticParams. contacto/ + showroom/ = own folders, excluded from [pageSlug].
- FOOTER RECOLUMN (AC-10): 2 groups → 3 real link columns. STORE_LINKS=[about] (+ showroom contextual link "Visita nuestro showroom →" on store-info block); HELP_LINKS=[faq,shipping,returns,contact]; NEW LEGAL_LINKS=[warranty,privacy,terms]. lg grid → 4 tracks. CHANGE footer.links.shipping label "Envíos y devoluciones"→"Envíos". NEW keys: footer.links.{returns,warranty,privacy,terms,showroom} + footer.sections.legal. nav-items.ts unchanged (/contacto verify-only). ZERO dead links after.
- HERO CONCEPT: editorial split — copy-left/image-right on lg (grid lg:grid-cols-2 gap-8 items-center), stacked copy→CTA→image on mobile. h1 = text-3xl sm:4xl lg:5xl tracking-tight text-balance (one step up from page h1). Placeholder image via HERO_IMAGE config asset in /public; MUST degrade to token panel (bg-muted rounded-lg aspect-[4/3] + hugeicons chair glyph text-muted-foreground/40 aria-hidden) if null — NO broken <img>. CTA = existing Button asChild → /sillas; secondary "Marcas →" reuses .link-arrow/.group/brands. Hero mount = .enter-fade. Always renders (AC-9).
- FEATURED: FeaturedProducts wraps ProductGrid VERBATIM (grid-cols-2 md:3 lg:4 — 2-col mobile, NOT 1-col; catalog consistency wins over ticket's 1-col suggestion). N=HOME_FEATURED_PRODUCTS=8 (2 full lg rows, no ragged row). FeaturedBrands wraps IndexTile+BrandLogo (grid cols-1 sm:2 lg:3, matches marcas). M=HOME_FEATURED_BRANDS=6. Both guard-return null if empty AND page omits (AC-9 belt+suspenders). HomeSectionHeader (tiny shared) = "{heading} … Ver todas →" (.link-arrow). Data: listProducts({pageSize:8}) + listBrands().slice(0,6) parallel.
- FAQ PATTERN DECISION: HEADED LIST, not accordion. Justification: zero client JS, native #id deep-link works on first paint (:target scroll-margin-top), maps 1:1 to plain-text ## question/paragraph-answer body, all answers Ctrl-F/SEO-visible, no toggle animation cost. FAQ = generic [pageSlug] route + StaticPageBody.
- StaticPageBody (NEW src/components/content/static-page-body.tsx): parse plain-text body — "## "-prefixed line → <h2 id=slugified> (deep-link), blank-line-separated → <p>. Render as React text children (auto-escaped). NEVER dangerouslySetInnerHTML (AC-17). div.max-w-prose.space-y-4. NO mount animation on prose (deliberate restraint — reading content shouldn't stage in).
- CONTACT FORM: single client island src/app/[locale]/contacto/contact-form.tsx, copies qa-form.tsx VERBATIM. Fields name/email/subject(optional)/message + honeypot (absolute left-[-9999px] + aria-hidden wrapper + tabIndex=-1 + autoComplete=off — invisible to users AND AT). fieldClasses string reused (no new field cmp). State: ContactFormState {status: idle|success|invalid|rate-limited|error; fieldErrors?; values?; submissionId} (contact-form-state.ts). Full state matrix designed: idle/validating/submitting(button disabled+"Enviando…")/success(role=status .enter-fade + clear inputs via submissionId re-key + focus + auto-hide SUCCESS_FEEDBACK_MS=6000)/error(role=alert + Retry, values preserved, raw reason NEVER shown)/rate-limited(role=alert amber, values preserved). Honeypot→status:success (fake). Live char counter aria-describedby. Contract: sendContactRelay({fromName,fromEmail,subject:string|null,message}). EMAIL_DEV_PREVIEW=1 makes success exercisable (QA path) + CONTACT_RATE_LIMIT_DISABLED=1.
- SHOWROOM (Option A): showroom/ folder, reads showroom body via StaticPageBody (## Dirección / ## Horario) + SHOWROOM_MAP_URL/SHOWROOM_MAP_IMAGE from config/static-pages.ts. grid lg:grid-cols-[1fr_minmax(0,340px)]. Static map <img> OR token panel if null; "Ver en mapas →" deep-link (rel=noopener, no SDK) omitted if link null; address/hours text ALWAYS renders (AC-18).
- MESSAGE KEYS (both es-MX.json+en.json lockstep, keys-used.test.ts enforces): NEW home.hero.* + home.featured.*, contact.* (full — labels/placeholders/submit/submitting/honeypot/charCount/success/errorGeneric/rateLimited/retry/errors.*), showroom.*, footer additions above. Reuse catalog.breadcrumb.{home,ariaLabel} for static-page breadcrumb chrome (don't duplicate). Full key inventory in ui-design.md.
- COMPONENT COUNTS: 7 new (Hero, FeaturedProducts, FeaturedBrands, HomeSectionHeader, StaticPageBody, ContactForm, ShowroomLocation) + 3 new routes ([pageSlug], contacto, showroom) + homepage rebuild; 1 modified (SiteFooter); 6 reused (Breadcrumbs, ProductGrid, ProductCard, IndexTile, BrandLogo, Button). All motion reuses shipped classes (.enter-fade/.stagger/.card-lift/.link-arrow), all ease-out enter, transform/opacity, prefers-reduced-motion gated.

=== S1 KEY DECISIONS (dev must honor) ===
- STATIC PAGES: 9 total. Currently only 4 seeded (sobre-nosotros, envios-y-devoluciones, preguntas-frecuentes, contacto). Must ADD returns/warranty/aviso-de-privacidad/terminos/showroom and RECONCILE the combined envios-y-devoluciones footer slug (site-footer.tsx STORE_LINKS) vs a shipping/returns split. Single-source the final slug set in NEW src/lib/config/static-pages.ts + reserved-slug guard (must not shadow sillas/marcas/categorias/estilos/carrito/checkout/producto).
- ROUTE SHAPE: ONE generic src/app/[locale]/[pageSlug]/page.tsx (generateStaticParams over slug set) for the 7 text-only pages; EXPLICIT contacto/ + showroom/ folders for the 2 bespoke ones. [pageSlug] is a dynamic segment sibling of static routes — App Router prefers static, but confirm no collision. Supersedes [...rest] catch-all for its slugs; missing/unpublished row → notFound().
- static_pages: id/slug(unique)/title/body(plain text, max 100k CHECK)/is_published(default true). RLS: anon select where is_published=true (0005). i18n via generic translations table (locale/entity_type='static_page'/entity_id/field/value) — NO translation rows seeded yet. NEW getStaticPageBySlug(slug,locale) in src/lib/content/static-pages.ts: createPublicClient, overlay en translation with es-MX base FALLBACK, degrade-to-null like store-settings.ts, unstable_cache tag 'static-pages'. Render body as ESCAPED paragraphs — NEVER dangerouslySetInnerHTML.
- SHOWROOM: store_settings has NO address/hours/map columns. RECOMMEND Option A — showroom copy in the showroom page body + map link/coords in config; NO migration. (Option B = additive store_settings cols via 0014, defer to Phase 2.)
- CONTACT FORM (wires T9 seam): copy Q&A grammar verbatim (src/app/[locale]/producto/[slug]/actions.ts + qa-form-state.ts + components/product/qa-form.tsx). submitContactForm(prevState,formData)=>ContactFormState (useActionState). honeypot→fake success; trim+length-cap+EMAIL_PATTERN validate (src/lib/contact/submit-guard.ts); rate limit src/lib/contact/rate-limit.ts (createSlidingWindowLimiter, key=clientIp(), CONTACT_RATE_LIMIT_* consts + CONTACT_RATE_LIMIT_DISABLED env); then sendContactRelay({fromName,fromEmail,subject,message}) (src/lib/email/dispatch.ts — template HTML-escapes body, sends to EMAIL_OWNER_ADDRESS, NOT order-ledger). ok:false → log reason, return status:error (never echo raw reason). NO zod in repo — pure validators. NO /api handler — server action.
- EMAIL ENV: EMAIL_* blocked-on-user; DEV/QA path = EMAIL_DEV_PREVIEW=1 (logs preview, returns ok). Live send is owner-gated (flag like T8 Phase 5). Success IS exercisable in preview mode.
- HOMEPAGE: replace T2 placeholder (page.tsx). hero (always) + featured-chairs (listProducts({pageSize:N}), reuse ProductGrid/ProductCard) + featured-brands (listBrands().slice(0,M), reuse IndexTile/BrandLogo). N,M named consts. OMIT a section on empty list (no empty grid). NO 'featured' DB flag — bounded slice of existing active queries. New src/components/home/{hero,featured-products,featured-brands}.tsx.
- i18n: storefront es-MX+en symmetric. Add staticPages/contact/home.featured/showroom namespaces to BOTH src/messages/es-MX.json + en.json (matching keys). NO hardcoded visible strings. footer.links currently: about/shipping/faq/contact.
- SEED: expand scripts/seed-data/content.ts 4→9 pages (structured placeholder es-MX; aviso/terminos = headed sections) + seed en translations rows in scripts/seed.ts (seeds none today); update seed-invariants*.test.ts counts. Idempotent upsert on slug.
- DEPS: none new. Static map = plain <img>/maps deep-link, no map SDK (CSP posture).

=== T12 COMPLETE — SHIP (Stage 12, quality 9/10, confidence HIGH, 2026-08-02) ===
- Pipeline: full-cycle HIGH, all 12 stages. S5 review 8.5/10 REQUEST-CHANGES (0 crit, 4 major) → S6 all 4 majors + 6 minors FIXED, 3/4 nits fixed (n-1 product-intent skip); S7 QA HIGH (AC 30/30, edges 11/11, +123 tests, 0 product bugs); S8 UX 9.5/10 (mobile-overflow crit fixed); S9 SECURE (0 crit/high/med, 2 accepted LOW, 0 secrets); S10 arch APPROVE-WITH-NOTES 9/10; S11 hacker chaos 1/10 (0 dead-UI/visual/logic/missing-state; 1 robustness gap FIXED = unbounded history-note → boundStatusNote).
- Final verified (Stage 12, INDEPENDENT — ran, not trusted): tsc 0, eslint clean (0 findings), fresh `supabase db reset` migrations 0001..0013 apply clean, prod build exit 0 + all 4 T12 routes present (/admin/orders, /admin/orders/[id], /admin/orders/[id]/packing-slip, /admin/orders/customers) + client-bundle server-symbol/secret scan 0 matches. Unit 1593/1593 (99 files). Integration 244/244 (22 files, official runner reseeds+pristine). AUTOMATED GRAND TOTAL 1837/1837. Own live AC spot-check (service-key vs reset DB) 11/11 PASS: cancel_order stock-restore variant 11→14, null-FK line skipped (edge11), idempotent re-cancel noop (no double-restore/no-dup-history), payment_status untouched=paid (edge6), history transition_kind='cancelled'; session-version bump 2→3 → stale-cookie v rejected (AC-27/28). Packing-slip 401 verified in code (401 before any read) + unit route.test.ts. GATES ALL CLOSED (none found open): AC-27/28 revocation (payload.v===currentVersion, fail-closed on null), AC-29 route self-guard, AC-30 all 5 actions requireSession() first, cancel_order transactional RPC, boundStatusNote hacker-fix present. E2E: authed-admin REFUND smoke owner-gated on live MP sandbox keys (T8 Phase 5) — accepted, does NOT block; ~8 Pixel-7 gotoPDP failures = documented harness flaw, excluded.
- WHAT EXISTS NOW: full admin order management under /admin/orders — order list (URL-synced search on order#/email/name meta-char-stripped + status/payment filters + 25/page pagination), order detail (contact/shipping/items/totals + status-pipeline stepper + chronological history log + tracking form + internal notes + action panel), manual status advance via advance_order_status (ONLY transition path) firing transition_kind-branched customer emails (shipped→sendShipped, cancelled→sendCancelled; email-fail ≠ rollback, surfaced as emailSent:false), tracking entry (empty#→null shipped email), transactional cancel_order RPC (0012: lock→per-line stock restore skipping null FKs→advance cancelled→history, idempotent, payment_status untouched so cancelled+paid stays refundable), full/partial MP refund (first caller of refundOrderPayment; over-refund refused by local pre-check + record_refund order-lock; raw MP error never echoed; sendRefundIssued once id-deduped; stable per-action idempotency key), self-guarded printable packing slip (401 unauth, CANCELADO band, escaped print-HTML, no PDF dep), customer list (bounded grouped-count RPC 0013), and dashboard new-order indicator (NEW_ORDER_STATUSES=[pending_payment,paid] single-sourced by count + ?new=1 link). SESSION REVOCATION (SEC-M-1 gate) LANDED: persisted admin_session_version singleton + bump_admin_session_version() RPC (0012), compared payload.v===currentVersion in async Node hasValidAdminSession() (fail-closed on read error); Edge pre-check unchanged (accepted defense-in-depth split); login stamps current version. Migrations now 0001..0013 (0012 admin orders, 0013 customer-count RPC); next is 0014.
- KEY FACTS FOR T13/T14: (1) ADR — inventory/money-critical multi-row atomic mutation = single SECURITY DEFINER SQL RPC (create_order, record_inventory_adjustment, cancel_order); app-level compensation retained ONLY for product-write (ADR-1, collapses at multi-admin trigger). If stock or money moves → SQL transaction, never app compensation. (2) ADR — session-version revocation is the multi-admin/per-user session foundation (ADR-2): crypto-free codec validates v is finite, persisted-equality at authoritative Node guard, bump = revocation not decode-failure, fail-closed. (3) NEW_ORDER_STATUSES single-source — any future "new order" surface consumes it, never re-lists. (4) T13 contact_relay template + send function exist (T9) but NOT live-wired — wire from the contact form.
- BACKLOG (non-blocking, tracked for a scale/cleanup ticket): customers(lower(email))/lower(full_name) indexes (0012) don't serve the ILIKE %term% query — drop or convert to pg_trgm; order-list search needs pg_trgm GIN before 10k+ orders (only true scale cliff); move ADMIN_SEARCH_MAX_LENGTH from products domain to a neutral admin module; fold AdminPagination into ListPagination (Boy-Scout on next product-list touch); refund idempotency-key server-derived (m-1, Phase-2); SEC-L-1 login stamp fail-closes on rotation-blip + SEC-L-2 refund-email newest-ledger-row race (both accepted LOW); 5 hacker product-improvements (multi-payment refund UI P1, bulk order actions P1, cancel undo P2, note char-counter P2, keyboard triage P3).
- DB STATE NOTE: local Supabase no longer holds the real PP-* orders — a prior stage reset it and this stage's integration runner reseeded (30 products, 0 orders, 0 customers, pristine). The owner's T8 Phase 5 live-refund test will need a FRESH order seeded (the PP-000005 3-dup / PP-000004 underpaid fodder is gone). admin_session_version=3 (bumped by this stage's live revocation spot-check) — harmless, no live admin cookie exists; next login stamps 3 and is valid.

=== T12 STAGE NOTES (kept for reference) ===
S11 PRIOR: Stage 11 Hacker (ultrahacker) COMPLETE — chaos score 1/10 (very robust). 0 dead UI / 0 visual bugs / 0 logic bugs / 0 missing states. 1 robustness gap found+FIXED: order_status_history.note is unbounded text with NO DB length CHECK, and cancel_order RPC + advanceOrderStatus insert the caller note verbatim (cancel reason is ALSO emailed) — client textareas only cap via maxLength (bypassable, inconsistent w/ order_internal_notes 1..2000 CHECK). FIX: added STATUS_NOTE_MAX_LENGTH=2000 (order-constants.ts) + boundStatusNote() helper (trim→null→slice cap) in orders/actions.ts applied to advanceStatus + cancelOrder; cancel-order-dialog.tsx maxLength now references STATUS_NOTE_MAX_LENGTH. Verified live: 0-item order cancelled w/ 2000-char reason → history note stored 1999 (clamped), no error. tsc clean, eslint clean (3 touched files), orders unit 102/102, all-admin unit 294/294. FULL CHAOS COVERAGE (live Playwright + SQL edge seeding, MP never called): URL tamper (page=-1/0/banana/99999/1e9, status=garbage, payment=' OR 1=1, new=banana, repeated params, page[] arrays) all bounded no-500; malformed detail ids (banana/<script>/truncated-uuid/nonexistent-uuid) → localized 404 never 500 (AC-7); XSS (<script>/<img onerror> in name/SKU/variant) escaped on detail AND packing slip, zero alert() fired; SQL injection in order+customer search meta-char-stripped; garbage input (emoji/RTL/zero-width/10k-char/HTML/whitespace) into notes+tracking+refund-amount all handled (server rejects 10k note, javascript: tracking URL rejected); refund modal garbage amounts disable Continue, over-refund pre-checked, REEMBOLSAR gate (lowercase ok), resets on reopen — NO MP submit; TWO-TAB advance race + double-click advance/note-save → exactly 1 write each (RPC FOR UPDATE + noop_same_status; useTransition disable); back/forward intact; rapid filter toggle converges; empty/0-item/no-orders/no-history states all present; button matrix correct (pending/empty refund-disabled, cancelled cancel-disabled+refund-enabled edge6, cancelled no-advance); logout revokes access; no dead links (detail 8/list 13/customers 6); no horizontal overflow 320-1280; packing slip print-optimized+CANCELADO band+no prices; customer-count join verified (1 for linked). 5 product improvements → hacker-report.md backlog (multi-payment refund UI P1, bulk order actions P1, cancel undo window P2, note char counter P2, keyboard triage P3). DB State: started 0 orders/0 customers (prior reset; real PP-* already absent, NOT deleted by me); seeded 7 CHAOS-* orders + 3 customers covering all edges; ALL DELETED after — DB pristine 0/0/0/0/0, no real PP-* touched, no MP refund call. Report: tasks/hacker-report.md (overwrote T11). Next: Stage 12 Verify (ultraverify) — final SHIP/NO-SHIP gate: full test suite, all 30 ACs, all reports. NOTE: refund money-path live-exercise still blocked on MP sandbox keys (T8 Phase 5, owner) — verify should confirm unit+integration cover it and flag the live smoke as owner-gated.
S8 PRIOR: Stage 8 UX (ultraux) COMPLETE — score 9.5/10, 7 issues found (1 crit / 3 major / 3 polish), 6 FIXED + 1 noted-justified. CRIT: mobile horizontal overflow on order detail — long unbroken email/address/product-name forced documentElement.scrollWidth=690 at 375vw (nearly 2x); root cause default flex/grid-item min-width:auto on the 2 grid columns + Panel section; FIXED with min-w-0 (both cols + Panel) + break-words on every free-text field (contact email/phone, shipping name/addr/delivery-notes, item name+SKU, history note, internal-note body); re-measured live scrollWidth=375 exact-fit. MAJORS: (1) phantom pb-24 md:pb-0 on [id]/page.tsx reserved ~96px for a mobile sticky-action-bar that is NOT implemented anywhere (grep inset-x-0 bottom-0/backdrop-blur→no match) — removed dead padding, actions flow at bottom of single-col stack (sticky bar descoped, documented); (2) customer-table long name/email overflow — max-w+break-words+align-top desktop, truncate mobile name; (3) tracking-form "Guía guardada" never auto-hid — added SAVED_FEEDBACK_MS=6000 auto-hide useEffect matching banner cadence. POLISH: break-words on history/internal notes + header email; NOTED: refund-modal reimplements MoneyField inline (justified — needs controlled value + live validity tint; a11y/visual parity intact; Phase-2 DRY). ALL edits className/structural + 1 timer → ZERO visible-copy change → zero E2E text-assertion risk. VERIFIED: tsc --noEmit clean, eslint clean on 5 touched files, orders unit suite 105/105 pass. LIVE PLAYWRIGHT (project-local) at 375/1024/desktop: login→list(desktop+mobile card split)→detail(cancelled+long-content mobile, overflow gone)→packing-slip(CANCELADO band, no prices, wraps)→dashboard(static amber tint, count=1). Seeded 2 orders (paid + cancelled/long-content) then RESTORED DB to pristine 0 orders; all temp scripts removed. A11y checklist ALL PASS (glyph+text badges, icon labels, modal focus trap+restore, role=status/alert async, disabled-refund aria-describedby, focus rings, table caption, tint contrast AA, prefers-reduced-motion on every animated surface). Motion audit 8/8 (◐ only: refund step1→2 instant swap vs spec blur-crossfade — acceptable). Consistency w/ T10/T11 = verbatim (AdminPage/table-card-split/nav-hover/badge/empty/filters-no-drawer/pagination/fields/dialog-content-motion) — indistinguishable from shipped. Report: tasks/ux-audit.md (overwrote T11). Next: Stage 9 Security (ultrasecurity) + Stage 10 Arch (ultraarch) IN PARALLEL — session-revocation trust boundary, refund money path, cancel_order RPC, /api/admin self-guards, packing-slip HTML-escape; arch = pattern compliance, data model, scalability.
S7 PRIOR: Stage 7 QA (ultraqa) COMPLETE — confidence HIGH, AC 30/30 covered + passing, 11/11 edge cases covered. +123 new tests (98 unit / 25 integration), 0 product bugs. FULL SUITE GREEN: unit 1593/1593 (99 files, was 1495 → +98), integration 244/244 (22 files, was 219 → +25), tsc --noEmit clean, eslint clean. NEW UNIT FILES (src/lib/admin/orders/, all created): packing-slip.test.ts(18 — HTML-escape hostile data/CANCELADO band/no prices), order-status-meta.test.ts(17 — offer map + deriveCancelledAt M-2 + transitionKindLabel), order-refund-write.test.ts(16 — key-thread AC-19/email-once AC-18/emailSent M-1/mp-error-no-echo AC-20), order-status-write.test.ts(14 — email branches on transition_kind never note AC-9/isolation AC-10), order-cancel-write.test.ts(11), order-tracking-input.test.ts(12 — empty#→null AC-12), order-refund-input.test.ts(10 — whole-peso→cents + int4 overflow), order-status-input.test.ts(8), + app/admin/(app)/orders/[id]/packing-slip/route.test.ts(6 — 401 no-read/404/500-friendly AC-22/29). NEW INTEGRATION FILES (tests/integration/, live local DB): admin-orders-cancel.integration.test.ts(11 — cancel_order RPC: both FK-path stock restore, payment_status untouched edge6, idempotent no-2nd-restore/no-dup-history, cancel-after-shipped edge3, since-deleted-FK skip edge11, anon denied), admin-customer-counts.integration.test.ts(8 — admin_customer_order_counts M-3: grouped counts, null-customer excluded, missing→0 omitted, request-scoped, anon denied), admin-orders-tracking-notes.integration.test.ts(8 — tracking cols mutable+null#, order_internal_notes CHECK/cascade/anon-deny 42501+insert-deny, refunded_total). PRODUCT CHANGE (Boy-Scout, behavior-preserving): extracted M-2 cancelled-band derivation from [id]/page.tsx inline JSX into pure unit-tested deriveCancelledAt() in order-status-meta.ts (+CancellableHistoryEntry type); page.tsx now calls it. STAGE-6 GAPS ALL CLOSED (customer-count RPC live, ?new=1↔dashboard single-source re-verified, cancelled-band derivation). 1 test-assertion bug self-caught+fixed (order_internal_notes anon deny is GRANT-level 42501, stronger than empty-RLS — assertion corrected). E2E: refund/cancel admin smoke SCOPED OUT this stage (refund leg needs live MP sandbox keys, T8 Phase-5 blocked-on-user; logic already covered at unit+integration) — documented recommendation for S11/S12 when keys land. Report: tasks/qa-report.md. Next: Stage 8 UX (ultraux) — load emil-design-eng + apple-design; audit list/detail/refund-modal/stepper states, a11y, responsive (375/768/1024), motion (ease-out enter, prefers-reduced-motion), es-MX copy.
S6 PRIOR: Stage 6 Fix (ultrafix) COMPLETE — ALL 4 majors + all 6 minors FIXED; 3/4 nits FIXED, n-1 SKIPPED (product intent). review-findings.md every finding marked FIXED/SKIPPED, all 30 ACs now PASS. M-1: onRefunded now (emailSent:boolean)=>void, threaded from result.emailSent through banner (refund-modal.tsx + order-detail-actions.tsx) + 3 new tests. M-2: cancelledAt derived from newest history 'cancelled' entry, null fallback ([id]/page.tsx). M-3: NEW migration 0013_admin_customer_order_counts.sql — grouped-count RPC (STABLE SECURITY DEFINER, empty search_path, service_role-only, anon denied); customer-list-query.readOrderCounts calls it (≤25 rows, no truncation); Args/Row types in rpc.ts. Verified: fresh db reset 0001..0013 clean + functional RPC check (2/1 counts, null-customer excluded, missing→0). M-4: chose "new = pending_payment OR paid" (AC-25 wording); single-sourced NEW_ORDER_STATUSES in order-list-filters.ts; dashboard count + new ?new=1 list seam both consume it; indicator link ?status=paid→?new=1; seam mutually exclusive w/ explicit ?status (status wins) + 10 new tests. Minors: m-2 randomKeySuffix() crypto.randomUUID guard/fallback; m-3 aria-invalid/aria-describedby on both refund inputs (step-2 passes error into TextField); m-4 bigint precision note in session-version.ts; m-5 TOCTOU comment in order-status-write.ts; m-6 copyNumber transient "Copiado" pill + logged rejection; m-1 documented/accepted. Nits n-2/n-3/n-4 fixed (stepper inline-style dropped + motion-reduce, hover-arrow RM gate x2, packing-slip docstring). VERIFY: tsc --noEmit clean; eslint clean on all touched files; unit 1495/1495 (was 1482, +13 new). MIGRATION ADDED: 0013 (next is 0014). Next: Stage 7 QA (ultraqa) — write integration/e2e coverage for the refund emailSent path, the ?new=1 filter + dashboard link agreement, the customer-count RPC, and the cancelled-band timestamp; 100% AC coverage.
S5 PRIOR: Stage 5 Review (ultrareview) COMPLETE — findings in tasks/review-findings.md. Verdict 8.5/10, REQUEST CHANGES (no critical, no block). 0 critical, 4 major, 6 minor, 4 nits. Backend is principal-grade: session revocation fail-closed + no downgrade path, refund idempotency correct end-to-end (mint-once in goToConfirm, stable on retry, unique per attempt), cancel_order proper single-tx RPC, both route handlers self-guard (AC-29), all actions requireSession() first (AC-30), packing-slip HTML-escaped, no injection/XSS/auth-bypass. MAJORS FOR ULTRAFIX: M-1 refund drops emailSent (order-detail-actions.tsx:162 hardcodes emailSent:true + refund-modal.tsx:110 onRefunded() no-arg → AC-10/edge7 gap on refund path only; advance+cancel thread it correctly). M-2 cancelled-band shows order.createdAt as cancellation time ([id]/page.tsx:37 → derive from history cancelled entry). M-3 customer order counts truncate at PostgREST 1000-row cap + unbounded fetch (customer-list-query.ts readOrderCounts → replace with grouped-count RPC). M-4 dashboard count(pending_payment+paid) vs link ?status=paid mismatch (align count/link). Also wire m-3 refund a11y aria-describedby/aria-invalid. Unmet/partial ACs: AC-10 (refund path), AC-24 (counts), AC-25 (dashboard). Next: Stage 6 Fix (ultrafix) — fix M-1..M-4 + m-3, re-run tsc/eslint/unit/integration, then Stage 7 QA.
S5 PRIOR: Stage 4 Dev (ultradev) COMPLETE. 47 files created + 10 modified. tsc clean, eslint clean (full repo), migration 0012 applies on fresh db reset (0001..0012), unit 1482/1482, integration 219/219, prod build green, 0 server symbols in client bundle. Full inventory + 30/30 AC coverage in tasks/dev-done.md. SESSION REVOCATION (AC-27/28) LANDED: persisted admin_session_version + bump RPC, enforced in async Node hasValidAdminSession() (Edge pre-check unchanged, by design); codec decoupled from version-equality (decodePayload accepts any finite v, guard owns equality). cancel_order RPC functionally verified live (stock restore both FK paths, idempotent noop, payment_status untouched=edge6, null-FK skip=edge11, transition_kind=cancelled). Refund = first caller of refundOrderPayment, two-step modal + stable idempotency key. Next: Stage 5 Review (ultrareview) — apply review-animations STANDARDS.md to the stepper/banner/modal motion; scrutinize the session-codec trust-boundary change, the refund path (money movement), and cancel_order transactionality. REVIEW RISKS flagged in dev-done.md "Risks for Review".
S4 PRIOR NOTES (kept for reference):
Stage 1+2 (PlanResearch) + Stage 3 (UI Design) COMPLETE. Complexity=high (auto-classify -> all 12 stages). Feature Type=full-feature / full-stack. Artifacts: tasks/next-ticket.md + tasks/research-report.md + tasks/ui-design.md. Load emil-design-eng before writing components; apple-design for refund modal / mobile drawers / stepper.
S3 KEY DECISIONS (dev must honor): (1) Reuse T11 grammar VERBATIM — AdminPage, ProductTable desktop-table/mobile-card split, ProductFilters debounced+URL-synced, AdminPagination, dropdown.tsx (already has dialog-content-motion), fields.tsx (MoneyField=$ adornment+inputmode=decimal for refund amount), dialog-content-motion class, .enter-fade for banners, .drawer-panel/.drawer-scrim for mobile sheets. (2) NO TOAST LIB in repo — success = close dialog + router.refresh() + inline success banner (role=status, .enter-fade, auto-hide 6s); NOT sonner. (3) Status badges = GLYPH+TEXT never color alone (mirror ProductStatusBadge); order_status glyphs ○◔◑◕●▢, payment glyphs ○◐●✕↩; single-sourced in NEW src/lib/admin/orders/order-status-meta.ts (labels+glyphs+variants+allowed-transition map+transition_kind labels — no magic strings in JSX). (4) Refund modal = TWO-STEP Dialog (compose → typed REEMBOLSAR confirmation gate), non-dismissable in-flight, stable idempotency key per open/submit cycle; Cancel = AlertDialog mirroring TaxonomyDeleteDialog. (5) Detail = section-composed, core always renders (history/notes fail → section-scoped banner, never 500). Stepper = 5 forward statuses; cancelled replaces stepper w/ band. (6) cancelled+paid keeps Refund ENABLED (edge 6). Partial refund leaves payment_status=paid — convey via Reembolsable balance line + ledger, never payment badge. (7) Packing slip = print HTML (@media print, window.print), CANCELADO band, self-guarded route 401. (8) 16 new components -> src/components/admin/orders/. All motion: enter --ease-out, exit faster, transform/opacity only, prefers-reduced-motion honored, hover gated behind hover-capable pointers.
Earlier context: Binding gates folded into ticket: AC-27/28 session-revocation (payload v), AC-29/30 /api/admin self-guard; cancel_order transactional RPC (0012); advance_order_status is only transition path; emails branch on transition_kind; refund.ts first-caller via MP (PP-000005 3 dup $6,999 / PP-000004 underpaid $2,499 = live refund fodder). T12 refund UI should use the duplicate sandbox payments on PP-000005 / underpaid PP-000004 as live test data when T8 Phase 5 resumes.

=== T11 COMPLETE — SHIP (Stage 12, quality 9/10, confidence HIGH, 2026-07-15) ===
- Pipeline: full-cycle HIGH, all 12 stages. S5 review 8/10 → S6 all 9 majors fixed+locked; S7 QA PASS HIGH (AC 35/35); S8 UX 9/10 (critical keyboard-tree fix); S9 SECURE (0 crit/high); S10 arch APPROVE 9/10; S11 hacker found+fixed int4-overflow (CRITICAL-class), variant double-submit, CSV blank-row bug — chaos score 2/10.
- Final verified (Stage 12, independent): tsc 0, eslint clean, build exit 0, unit 1469/1469 (87 files), integration 219/219 (19 files), e2e prod storefront chromium 39/39 + admin guard 20/20, e2e dev serial admin-products 46/46 + chaos 4/4 + admin core 30/30. GRAND TOTAL 427/427. Storage stop/start/reset cycle verified; migrations 0001..0011 clean; prod bundle 0 admin symbols.
- WHAT EXISTS NOW: full admin product management under /admin (products list w/ URL-synced filters+pagination, full product form, image manager [Supabase storage, magic-byte validation, pointer+keyboard reorder, cover], inline variant editor, taxonomy manager [nestable category tree, full ARIA APG keyboard pattern], inventory ledger + atomic record_inventory_adjustment RPC (0011), Q&A inbox, CSV import [RFC-4180 hand-rolled, dry-run stepper, per-row atomicity] + guarded export route). [storage] enabled in local config (analytics/edge stay OFF). Types hand-authored in types/ (repo convention). INT4_MAX guard at shared parser boundary. next.config protocol derived from NEXT_PUBLIC_SUPABASE_URL (http local only).
- T12 TEMPLATES (arch-verified): order list should adopt lib/admin/products/{list-query,list-filters}.ts + pagination.ts VERBATIM; writes = paired *-input.ts/*-write.ts; every action requireSession() first; cache namespaces catalog vs orders disjoint by construction; vitest.integration.config.ts has server-only→no-op alias for testing real write modules.

=== T12 GATES (binding — from T10+T11 security/arch; land BEFORE/DURING T12) ===
- SEC-M-1/ADR-2 REVOCATION: stateless admin sessions have NO server-side revocation (≤8h stolen-cookie window). MUST add session-version check (payload v field reserved) or shorten max-age BEFORE refund-capable T12 sessions.
- /api/admin/* is NOT middleware-guarded (matcher excludes /api) — T12 route handlers must self-call session check at entry (T11 export route is the template; currently NO /api/admin/* handlers exist).
- T12 cancel_order stock-restore = transactional SQL RPC per create_order (0008) precedent — NOT the T11 compensation pattern.
- T12 order-form contract goes in lib/admin/orders/ (avoid T11's type-only lib→app inversion).
- Wire T9 email seams: shipped/cancelled/refund_issued templates + send functions exist tested NOT live-wired; refund.ts (T8) server-only, called nowhere until T12 auth-gates it. Emails branch on order_status_history.transition_kind — never string-match note text.
- advance_order_status RPC (0009) is the ONLY status-transition path; payment-only mode (p_order_status=null) for refunds. record_refund RPC + payment_refunds ledger exist.

=== BACKLOG (non-blocking, tracked) ===
- SEC-T11-M-1: extend UUID_PATTERN guard uniformly to all client-supplied entity ids (currently parameterized .eq only — safe, single-owner).
- Mobile gotoPDP e2e harness strict-mode flaw (tasks/clean-code-backlog.md) — ~8 pre-existing Pixel-7 failures, not a product bug.
- ADR-1 compensation→create_product_with_links RPC at multi-admin trigger (Phase 2); closes CSV/product-write ~120-line DRY seam.
- CI should gate client-bundle secret scan on prod build (SEC low).
- 5 product-improvement suggestions in tasks/hacker-report.md (feed future tickets).

=== STANDING GATES (carry forward — do not drop) ===
- HUMAN-REVIEW GATE UPDATE (2026-07-23): owner completed action-plan Phases 1–4 (setup, happy-path purchase, cheat spot-checks: oversell, discount abuse, discount math, token tampering, double-submit) — ALL PASSED. T7 gate CLOSED → T7 checked off in BUILD_PLAN. T8 stays UNCHECKED: sole remaining gate is the live MP sandbox test (action-plan Phase 5), blocked-on-user (placeholder MERCADOPAGO_* keys in .env.local). Owner may alternatively declare Phase 5 "consciously scheduled before go-live" (Phase 7 wording) to close T8 early — their call, do not assume.
- T8 remaining focus when keys arrive: 4 live flows (approved card, OXXO/SPEI pending→paid, declined card, refund [defer to T12 button]) + replayed-webhook rejection.
- T8 PHASE 5 IN PROGRESS (2026-08-01, prev 2026-07-28): MERCADOPAGO_* keys in .env.local (test-user account — no live-money risk). SHIPPING BUG FOUND+FIXED (commit 3e54e02): preference omitted shipping → under-charge → webhook amount-mismatch → orders stuck pending (PP-000004). Preference now charges Envío line / collapses to order-total line if lines≠total. Verified live: PP-000005 + PP-000006 charged exact totals, flipped to paid via locally-SIGNED webhook replays through the real route (processing logic proven end-to-end with real approved payments). SOLE REMAINING BLOCKER: MP panel now delivers type=payment webhooks to the tunnel but they 401 signature_mismatch — panel test-mode signing secret ≠ MERCADOPAGO_WEBHOOK_SECRET we hold. Owner: copy Clave secreta from panel Webhooks (test mode) → .env.local → restart → one more Flow 1 payment should auto-confirm ⇒ Flow 1 checked. Then Flows 2, 3, replay check; Flow 4 refund deferred to T12 (note: PP-000005 has 3 dup approved $6,999 payments + PP-000004 underpaid $2,499 — live refund fodder for T12). ALSO: next.config.ts allowedDevOrigins (from NEXT_PUBLIC_SITE_URL) added — next dev blocked cross-origin /_next from the tunnel host so pages never hydrated (dead buttons); UNCOMMITTED. Ngrok domain safari-education-capillary.ngrok-free.dev is the account's RESERVED static domain — restart with `ngrok http --url=safari-education-capillary.ngrok-free.dev 3000`, same URL every time, no panel/env churn. Full detail in tasks/review-action-plan-T7-T8.md 2026-08-01 note.
- T9 checkout/payment diffs (bdd37bc, 6c19265) + clean-code A4 commit were included in the reviewed scope.
- T9 LIVE-SEND blocked-on-user: no EMAIL_* vars in .env.local (EMAIL_API_KEY/Resend, EMAIL_FROM_ADDRESS, EMAIL_OWNER_ADDRESS, NEXT_PUBLIC_SITE_ORIGIN; dev EMAIL_DEV_PREVIEW=1).
- ADVISORY: user may want to eyeball the admin auth core (src/lib/admin/{session,session-edge,auth}.ts + middleware /admin branch) — 0 crit/high across two dedicated security stages, but it IS the trust boundary.
- T13 seam (from T9): contact_relay template + send function exist, NOT live-wired — wire from contact form.

=== ADMIN FACTS (stable) ===
- Auth: HMAC-SHA256 HttpOnly cookie Path=/admin, es-MX only, locale-free /admin tree, defense-in-depth (Edge middleware verify → (app) layout node:crypto verify → per-action requireSession). Dev creds: admin@posturpro.mx / posturpro-dev-2026. ADMIN_PASSWORD_HASH in .env*: every $ MUST be backslash-escaped (dotenv-expansion P1; dev-only format guard fails fast; hash-gen snippet in T10 dev-done emits pre-escaped).
- New admin sections = flag-flip in src/lib/admin/constants.ts + route dirs under src/app/admin/(app)/.
- Integer cents/MXN; storefront i18n es-MX+en symmetric; admin es-MX only.

=== E2E/ENV INFRA (binding) ===
- LOCAL Docker Supabase only; remote empty/unlinked — never remote push. Migrations 0001..0013 (0012 admin orders, 0013 admin_customer_order_counts RPC); next is 0014. [storage] ON, [analytics]/[edge_runtime] OFF.
- Authoritative storefront e2e: PROD build, seed BEFORE build (reseed → rebuild; stale dist cache serves dead variant UUIDs → order e2e stuck at /checkout with 0 orders — NOT a Server-Action bug), NEXT_QA_DIST_DIR, CHECKOUT_RATE_LIMIT_DISABLED=1, `git checkout -- tsconfig.json` after.
- Authed admin e2e: DEV server (next start forces Secure cookie, rejected over plain HTTP — correct behavior), FRESH server + FRESH seed, serial --workers=1, ADMIN_LOGIN_RATE_LIMIT_DISABLED=1. Unauth-guard tests run on prod build.
- KNOWN FLAKES (pre-existing): mobile gotoPDP strict-mode (backlog); cross-project stock-depletion race (run per-project, reseed between); payment-panel unit flake (passes isolated); 2 moderate postcss advisories (transitive via next); admin first-run stale-cache on REUSED dev servers (fresh-server rule avoids).
- Test baseline after T11: unit 1469/1469 (87 files), integration 219/219 (19 files), e2e storefront chromium 39/39 prod + admin guard 20/20 prod + admin 30/30 + admin-products 46/46 + chaos 4/4 (dev serial). DB pristine = 30 seed products, 0 orders/ledger/storage-objects/questions.
- caffeinate still running from interactive session; port 3000 clear; no dev server running.
