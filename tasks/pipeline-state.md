# Pipeline State
Task: T10 — Admin foundation
Tier: full-cycle (medium) — skip Stage 11 (Hacker); Security (9) + Arch (10) at FULL depth (trust boundary)
Stage: 5
Agent: ultrareview
Last Updated: 2026-07-15
Notes: After T10 ships, continue immediately with /full-cycle for T11 — Admin: product management (user is AFK; run both autonomously end-to-end).
=== S4 (ultradev) COMPLETE — tasks/dev-done.md written. Built self-managed HMAC-SHA256 signed HttpOnly session-cookie auth (NOT Supabase Auth): getAdminEnv() in env.ts; src/lib/admin/{constants,session-payload,session,session-edge,auth,login-rate-limit,settings-input,session-guard}.ts (+ 7 colocated tests); locale-free /admin tree with parallel root layout + (app) route-group guard sub-layout; login/settings pages + actions.ts (login/logout/saveStoreSettings) + admin-form-state.ts; components/admin/{admin-shell,admin-nav,logout-button,admin-page,login-form,store-settings-form}. Defense-in-depth: middleware /admin branch (Edge crypto.subtle verify, R1) returns BEFORE next-intl (R2 storefront untouched) + (app) layout node:crypto verify + saveStoreSettings re-verify. updateStoreSettings() write in store-settings.ts busts cache via updateTag (Next16 replaced revalidateTag single-arg). NO migration (AC-14 confirmed; store_settings singleton+CHECKs from 0003/0006). DEVIATIONS: route-group (app) for guard vs sibling login; updateTag vs revalidateTag (Next16 deprecation). VERIFIED: tsc 0, eslint clean, next build exit 0 (dist dir removed, tsconfig restored), unit 1342/1342 (75 files; +61 admin) — the lone payment-panel flake is pre-existing/unrelated (passes isolated + on re-run). Live dev-server curl: /=200, /en=200, /admin=307→login (no markup), /admin/login=200, /admin/settings unauth=307→login, /admin/=308. .env.local has ADMIN_EMAIL/ADMIN_PASSWORD_HASH(scrypt, dev pw posturpro-dev-2026)/ADMIN_SESSION_SECRET (all server-only). Auth+session round-trip verified at Node level against real env. FOR REVIEW: apply review-animations STANDARDS to enter-fade/drawer reuse (all reused, no new motion CSS); scrutinize the R1 Edge/Node verify split + timing-parity dummy-hash + no-enumeration login + cookie flags (HttpOnly/Lax/Secure-in-prod/Path=/admin).
=== S3 (ultradesign) COMPLETE — tasks/ui-design.md written (overwrote T8 spec per convention). Sidebar shell ≥768px + top bar/drawer <768px reusing storefront .drawer-panel/.drawer-scrim (--ease-drawer). /admin redirects to /admin/settings (no dead dashboard); nav = AdminNavItem[] in constants.ts with status live/soon — T11/T12 enable by flag flip. Zero new shadcn components / motion CSS; forms follow qa-form.tsx / checkout-flow-client.tsx canon (FieldError, pending label-swap, aria-invalid/describedby, role=alert/status). Money: inputmode=decimal, static $ adornment, tabular-nums, strict ^\d+(\.\d{1,2})?$ parser in pure settings-input.ts (strips leading $, rejects thousand separators, accepts 0/0.00, guards cents overflow). State contracts AdminLoginState/AdminSettingsState in admin-form-state.ts mirroring checkout-form-state.ts split rule. Full es-MX copy + a11y checklist + motion table (enter-fade only on occasional surfaces, RM handled by reused classes). Login states: generic bad-creds banner (no enumeration per AC-3), rate-limited, env-unavailable; settings: row-missing+seed-defaults, field invalid, DB error, unauth re-verify.
=== S1+2 COMPLETE — Complexity MEDIUM, Feature Type FULL-STACK. Auth: self-managed HMAC-SHA256-signed HttpOnly session cookie (NOT Supabase Auth); ADMIN_EMAIL + ADMIN_PASSWORD_HASH (scrypt) + ADMIN_SESSION_SECRET via getAdminEnv() in src/lib/env.ts, server-only. Defense-in-depth: middleware guard + layout guard + per-action re-verify. Admin = locale-free /admin sibling tree (own root layout, no next-intl), es-MX only, cookie Path=/admin. KEY RISKS: R1 middleware is Edge runtime → Web Crypto (crypto.subtle) verify there, node:crypto authoritative verify in layout/actions; R2 middleware change must not regress storefront locale/cart (full e2e prod build); R3/R5 login user-enumeration/blank-hash → dummy-hash timing parity, generic errors, missing-env throws; R7 money input parsing → strict pure parser + tests. NO new deps, NO migration expected (store_settings singleton from 0003/0006 already has CHECKs + updated_at trigger; STORE_SETTINGS_CACHE_TAG bust seam pre-wired in src/lib/store-settings.ts).

=== STANDING GATES (carry forward — do not drop) ===
- HUMAN-REVIEW GATE (BUILD_PLAN rule 3): T7 and T8 both have advisory SHIP verdicts but remain UNCHECKED in BUILD_PLAN.md until the user manually reviews. Do not check them off.
- T7 human-review focus: 0008_checkout.sql create_order SECURITY DEFINER fn; checkout server-action trust boundary (actions.ts); rate-limit.ts best-effort caveat + CHECKOUT_RATE_LIMIT_DISABLED unset in real deploys; money math (order.ts, discount.ts, cart/shipping.ts).
- T8 human-review focus (from ship-decision.md): webhook signature verification, amount reconciliation, refund execution, RPC transition/idempotency, secret handling.
- T8 LIVE-SANDBOX verification BLOCKED-ON-USER (only placeholder MERCADOPAGO_* keys in .env.local; all tests mock MP).
- T9 rule-3 addendum: T9 modified the checkout action (src/app/[locale]/checkout/actions.ts — locale persistence + email triggers) and src/lib/payments/process-payment.ts (transition_kind email dispatch) — include these diffs (bdd37bc, 6c19265) in the same human review pass as T7/T8. T9 itself is checked off (QA gate passed); the review flag is about the touched checkout/payment files.
- Clean-code A4 addendum (2026-07-14): the A4 refactor commit also touches the flagged checkout files — pure form-parsing helpers extracted verbatim from actions.ts to src/lib/checkout/form-parsing.ts, and both local clientIp copies replaced by the canonical src/lib/request/client-ip.ts after a proven behavior-identical diff (closes SEC-M-1, zero rate-limit keying change). Include the A4 commit in the human-review pass; verified by checkout e2e 24/24 on prod build.
- T9 LIVE-SEND verification BLOCKED-ON-USER: no EMAIL_* vars in .env.local; all tests mock the provider. Env vars documented in dev-done.md: EMAIL_API_KEY (secret, Resend), EMAIL_FROM_ADDRESS, EMAIL_OWNER_ADDRESS, NEXT_PUBLIC_SITE_ORIGIN; dev preview via EMAIL_DEV_PREVIEW=1.

=== T9 SEAMS FOR LATER TASKS ===
- T12 (admin): shipped / cancelled / refund_issued templates + send functions exist, tested, NOT live-wired — wire from admin actions. refund.ts still called nowhere until T12 auth-gates it.
- T13 (contact page): contact_relay template + send function exist, tested, NOT live-wired — wire from the contact form.
- Emails branch on order_status_history.transition_kind (RPC-returned) — never string-match note text.

=== T8 FACTS LATER TASKS BUILD ON ===
- advance_order_status RPC (0009) is the ONLY status-transition path; writes order_status_history; payment-only mode (p_order_status=null) sets payment_status=refunded with order_status untouched (from==to in history — hence TD-2).
- Webhook processing: record_payment_event claim-then-finalize spine keyed (mp_payment_id, mp_status); callers branch on result.reason (advanced/payment_updated/noop_same_status = success).
- payment_refunds ledger + record_refund RPC exist; refund.ts is server-only, called nowhere until T12.
- Confirmation page addressed by orders.confirmation_token at /[locale]/checkout/confirmacion/[token]; confirmationPath(token) in config.
- Integer cents everywhere; currency MXN; i18n es-MX + en both locales symmetric.

=== E2E/ENV INFRA (binding) ===
- LOCAL Docker Supabase only; remote empty/unlinked — apply new migrations locally, never remote push.
- Authoritative e2e: PRODUCTION build (next build + next start), separate NEXT_QA_DIST_DIR, CHECKOUT_RATE_LIMIT_DISABLED=1. Reseed between order-placing suites; leave DB pristine. After any prod build with NEXT_QA_DIST_DIR: `git checkout -- tsconfig.json`.
- SEQUENCING RULE (root-caused 2026-07-14, bisect after T9 QA): on PROD builds, ALWAYS `supabase db reset` + seed BEFORE `next build`; after ANY reseed, REBUILD (server restart alone is unreliable — stale dist-dir cache can persist). Why: db reset mints fresh product/variant UUIDs while the prod server serves cached page data embedding the OLD variant UUIDs → add-to-cart stores a dead variant id → create_order rejects → order-placing e2e fails with URL stuck at /checkout and 0 orders. Dev servers render per-request, so dev passes — do NOT mistake this signature for a Server-Action/FormData bug (that diagnosis was disproven).
- Test baseline after T9: unit 1281/1281 (68 files), integration 180/180 (13 files), e2e payment 8/8 + checkout 24/24 + cart 46/46 (prod build, correct seed-before-build sequencing). Migrations 0001..0010.
- NOTE (2026-07-15): a dev server (npm run dev, background task blhgnwi11) and caffeinate are running from the interactive session — kill/ignore the dev server as needed for QA prod builds; leave caffeinate alone.
