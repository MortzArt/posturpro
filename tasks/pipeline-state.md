# Pipeline State
Task: T8 — Mercado Pago integration (sandbox)
Tier: full-cycle
Stage: 12 COMPLETE (Verify) — T8 PIPELINE COMPLETE (advisory SHIP; gates OPEN)
Agent: none (pipeline done) — awaiting HUMAN REVIEW + live-sandbox before merge
Last Updated: 2026-07-14
Notes: Stage 12 (ultraverify) COMPLETE — tasks/ship-decision.md written. VERDICT: SHIP (ADVISORY), Confidence HIGH, Quality 9/10. Verify ran everything itself: eslint src/ clean, tsc --noEmit 0 errors, next build (NEXT_QA_DIST_DIR=.next-t8-verify) clean both locales (ƒ /api/webhooks/mercadopago emitted, ƒ confirmacion/[token] dynamic). SUITES PERSONALLY RUN: unit 1206/1206 (60 files); integration 158/158 (scripts/run-integration.sh reset+seed, migrations 0001..0009 clean); e2e on PROD build (next start on .next-t8-verify, CHECKOUT_RATE_LIMIT_DISABLED=1, reseeded between order-placing suites): payment 8/8, checkout 24/24, cart 46/46 = 1442 total green, 0 failures. HIGH-RISK ACs SPOT-CHECKED IN CODE (all sound): webhook fail-closed on unset secret (two independent gates), authoritative Payment.get (body status never trusted), C-1 query-id-only manifest, zero-tolerance amount reconciliation gating only the paid transition, advance_order_status the ONLY status write path (no .update({status}) in payment code), (id,status) dedupe + claim-then-finalize, over-refund guard race-safe (for update + ledger sum), refund server-only + per-attempt UUID idempotency key (H-1), no NEXT_PUBLIC secret / secret only in server-only+env+test modules, 0009 all 6 RPCs security definer + empty search_path + service_role-only, migration local-only idempotent. 4 HACKER FIXES ALL PRESENT + regression-locked (StaleCard not-payable; rate-limited honest amber copy; refund per-attempt key; refunded-on-never-paid neutral copy). CLEANUP: prod server killed, .next-t8-verify removed, tsconfig restored (no dist globs, only standard .next/types), DB pristine-seeded (0 orders), tree CLEAN (git status empty; git ls-files|grep .next → only next-env.d.ts). Also swept pre-existing leaked dirs .next-t5-ux + .next-t8-hacker (both gitignored, never tracked) + stray untracked playwright.hacker.config.ts. DISCREPANCIES FOUND (none block ship): (1) tasks/hacker-report.md ON DISK IS THE T7 REPORT — never overwritten for T8 (last touched by commit 0708911); T8 hacker work IS committed (4474f8b, 13 files) with fixes+tests present & passing — a DOCUMENTATION gap only, regenerate hacker-report.md for T8 before archiving. (2) Task prompt cited commit d23eff0 which is NOT in history — real Stage-11 T8 commit is 4474f8b (diff matches described work). === STAGE 11 (prior) COMPLETE — tasks/hacker-report.md written. Chaos score 3/10 (PASS). HUMAN-REVIEW GATE stays OPEN (advisory pass only). Trust boundary + money math held under adversarial webhook chaos (unsigned/wrong-secret/replay/out-of-order/10-concurrent-identical/amount-tampered/unknown-id/foreign-external_reference/malformed-body/lying-content-length/weird-x-signature/stale-ts/MP-down all SOUND: (id,status) spine + `on conflict do nothing` + `for update` row lock + idempotent advance ⇒ exactly one history row; spoofed ?mp_status never flips DB truth; double-click Pagar disabled via useTransition; 320px voucher break-all sound). ALL FRAGILITY WAS IN CLIENT RECOVERY-UX FOR NON-HAPPY PATHS — 4 bugs FIXED + regression-locked: (1) not-payable → infinite false "declined" loop: when a webhook/2nd-tab marks the order paid mid-session, "Pagar ahora" returns not-payable which rendered role=alert "Tu pago fue rechazado" + a Retry wired to the same action (loops forever, never reveals paid). FIX: new StaleCard (payment-panel-stale, role=status) "El estado de tu pedido cambió" + Reload button revealing authoritative state; new i18n checkout.payment.stale.* both locales. (2) rate-limited (SEC-H-1 limiter) → same false "declined": FIX honest amber "Demasiados intentos, espera un momento" via parameterized UnavailableCard; new checkout.payment.rateLimited.* both locales. (3) refund idempotency-key collision: MP X-Idempotency-Key was refund:${order}:${amount} → two distinct partial refunds of equal cents collide at MP, second silently collapsed (no 2nd money moves) but recorded as fresh → FIX per-attempt UUID/caller-threaded key. (4) refunded on a never-paid order showed "Payment received · Refunded" → FIX panel-state.ts falls through to neutral unpaid copy. Regression-locked with 3 new panel tests (stale-reveals-reload; not-a-decline; rate-limited-not-a-decline) + refund/panel-state tests. VERIFIED BY ORCHESTRATOR (hacker left tree UNCOMMITTED + flagged a "concurrent process" wrote refund.ts/panel-state.ts — orchestrator confirmed the whole tree coherent before committing): tsc 0 errors, eslint clean, unit 1206/1206 (was 1192, +14), integration 158/158 (was 154, +4 — payments.integration.test.ts +105 lines of webhook-chaos/refund coverage). NOTE: hacker did NOT run the prod-build e2e sweep (Verify Stage 12 owns the authoritative e2e). 4 product ideas listed (NOT implemented — phase discipline): P2 cache voucher fields at webhook time (confirmation page currently blocks up to 8s on live Payment.get in Promise.all — extends Arch TD-6); P2 auto-refresh processing/pending panel so webhook-before-redirect race self-resolves; P3 stale card uses router.refresh not location.reload; P2 not-payable returns why (already-paid → "¡Ya está pagado!"). Committed by orchestrator (see stage-11 commit).
=== prior stages === Stages 9+10 COMPLETE. SECURITY (tasks/security-audit.md): verdict SECURE (advisory — human-review gate stays open). 0 critical, 1 HIGH FIXED: SEC-H-1 — createPaymentPreference (unauthenticated "use server" action) had NO rate limit; each call = a live billable MP Preference.create + 2 DB reads; one leaked confirmation_token → amplification/DoS vs MP quota, breaking checkout for real shoppers. FIX: per-IP sliding-window limiter 10/min/IP checked BEFORE any DB read or MP call; new typed `rate-limited` result rendered as the panel retry banner; mechanics extracted to shared src/lib/rate-limit/sliding-window.ts (T7 checkout limiter now delegates, API unchanged, tests green) + canonical src/lib/request/client-ip.ts; new preference-rate-limit.ts + 7 tests; reuses CHECKOUT_RATE_LIMIT_DISABLED e2e escape hatch. Accepted: SEC-M-1 (clientIp trust model duplicated in the two shipped T7 action files — left untouched in a security-only stage, unify later), SEC-L-1 (attacker-influenced payment id logged plaintext post-HMAC-gate), 2 pre-existing moderate dev-only npm-audit findings. VERIFIED IN CODE: fail-CLOSED when MERCADOPAGO_WEBHOOK_SECRET unset/empty (two independent gates); leaked-secret blast radius bounded (authoritative Payment.get + zero-tolerance amount reconciliation + dedupe spine + 5-min replay window + regression guard — cannot mark unpaid paid without a real correctly-priced MP payment); refund server-only + called nowhere (correct until T12 auth); RPCs SECURITY DEFINER + search_path='' + service_role-only; 0 secrets in git/history; mercadopago@3.2.0 no known CVE; no PAN stored. Suites after fix: tsc 0, eslint clean, unit 1192/1192 (+6), build clean. ARCH (tasks/architecture-review.md): grade A (9/10), APPROVE (advisory). R-1 ✅ (grep-confirmed: advance_order_status is the ONLY status-write path), R-3 ✅ (spine re-key + claim-then-finalize exceeds the ask), R-4 ✅ (partial indexes + ledger indexes). Strengths: one audited money boundary, crash-durable claim-then-finalize (no stuck-order path found), fail-safe on unknown/new MP statuses, race-safe cumulative-refund guard. RECOMMENDATIONS: (TD-2, BEFORE T9, effort S) order_status_history lacks a structured transition_kind — payment-only refund rows have from==to; add typed column/RPC-return BEFORE T9 hardcodes note string-matching. (TD-1, BEFORE T12, effort M) no durable needs-review surface — chargebacks/mediation/unknown-status/amount-mismatch are console.error+200 only; add queryable flag before admin exists. (TD-3, flag for T12) cancel-with-stock-restore does NOT compose free — needs ONE atomic restock+cancel RPC, not two calls; expired-voucher orders leak stock until a cancel/sweep flow exists. Also: refund fn trusts caller for auth (T12 must gate); cancelled is terminal by rank design; amend-in-place migration hygiene stops being safe on first non-resettable env (TD-5).
=== prior stages === Stage 8 (ultraux) COMPLETE — verdict SHIP (ADVISORY), UX score 9/10. tasks/ux-audit.md has findings by severity + fixed/deferred + a11y/keyboard/responsive results + m-6 resolution + full suite results. FIXES THIS STAGE (13 files, +9 net unit tests → 1186): (1) 🔴 PAID METHOD LABEL BUG — read-side toPaymentMethodKey re-ran the MP-type heuristic on an already-stored compact key, so paid CARD/WALLET orders showed generic "Pago confirmado" instead of "Pagado con tarjeta"/"Pagado con Mercado Pago" (OXXO/SPEI survived via method_id short-circuit). Added parsePaymentMethodKey() in config.ts (validates a stored compact key against PAYMENT_METHOD_KEYS); order-payment-read.ts uses it. Locked with 3 config.test.ts cases. NO prior test protected this (panel test fed method:"card" directly, bypassing the read). (2) 🟡 EXPIRED-VOUCHER COPY — a failed OXXO/SPEI (expired voucher) was mislabeled "Tu pago fue rechazado" (declined). derivePanelState now emits {kind:"failed", reason:"declined"|"expired"} (voucher-method→expired); FailedCard picks copy by reason + data-failure-reason attr; added expired.title both locales, activating the previously-DEAD payment.expired.body key. Locked with 2 panel-state tests + 1 component test. (3) 🟢 sm:w-auto was DEAD on the pay/retry/view-voucher CTAs (parent flex-col align-items:stretch overrode width:auto → full-bleed black bar on tablet/desktop); added sm:self-start so CTAs are content-width on ≥sm, full-width thumb target on mobile. (4) 🟢 degraded voucher showed two redundant email lines (generating + no-url) → no-url now only shows when a reference exists; added house focus-visible ring to the 4 plain buttons (were UA-default outline). === m-6 RESOLUTION: SIGNED OFF, NO CODE CHANGE. binary_mode:false is a hard requirement (true auto-rejects OXXO/SPEI). The card-in-process consequence maps to the `processing` state, which I audited live (amber Clock, "Estamos confirmando tu pago", Actualizar reload + Reintentar escape, role=status) — correct, non-alarming, wayfinding-complete UX. Confirm end-to-end at the live sandbox (BLOCKED-ON-USER). === VERIFICATION: unit 1186/1186 (1177 + 9 new), integration 154/154 (scripts/run-integration.sh; note: seed hits a transient postgREST schema-cache race right after db reset — warm postgREST before seeding, then tests pass), e2e payment 8/8 + checkout 24/24 on the PRODUCTION build (NEXT_QA_DIST_DIR=.next-ux-qa on port 3100, CHECKOUT_RATE_LIMIT_DISABLED=1, reseeded between suites — payment e2e failures on the DEV server were order-placement flakiness, NOT a regression: the panel/unavailable/EN/no-overflow all pass on prod). tsc 0; eslint clean; next build clean (webhook route ƒ /api/webhooks/mercadopago). CLEANUP DONE: .next-ux-qa removed, temp harness (.ux-shoot.mjs) + temp playwright.ux.config.ts removed, test-results removed, tsconfig.json restored (git checkout), DB pristine-seeded (0 synthetic orders), no stray servers. === FORWARD FACTS FOR SECURITY (9) + ARCH (10): UX changed ONLY UI copy/layout/focus — no webhook/RPC/refund/signature/reconciliation logic touched. New public export config.ts::parsePaymentMethodKey (pure, read-side, no I/O). New panel-state field FailureReason on the "failed" kind. No new deps, no migration change, no new secret, no new route. data-testids + a11y roles all preserved (QA regression locks intact). HUMAN-REVIEW GATE + BLOCKED-ON-USER live sandbox both remain OPEN regardless of the SHIP verdict. === Stage 7 (ultraqa) COMPLETE — verdict SHIP (ADVISORY), confidence HIGH. tasks/qa-report.md has the 23/23 AC matrix, 12/12 edge-case matrix, and a regression-lock table for every Stage-6 C/M finding (all discriminate the exact failure mode). SUITES ALL GREEN: unit 1177/1177 (baseline 1126 + 51 new), integration 154/154 (baseline 151 + 3 new) via scripts/run-integration.sh reset+seed, e2e payment 8/8 + checkout 24/24 + cart 46/46 (each on the PRODUCTION build with NEXT_QA_DIST_DIR=.next-t8-qa + CHECKOUT_RATE_LIMIT_DISABLED=1, run/reseeded in isolation — the two order-placing suites deplete the same Milano-chair stock so they MUST be reseeded between runs; this is the documented infra rule, NOT a regression). tsc 0 errors; eslint clean; next build clean (webhook route ƒ /api/webhooks/mercadopago). CLEANUP DONE: QA dist dir removed, tsconfig.json restored (git checkout), DB left pristine-seeded, no stray servers. ZERO known bugs. === NEW TESTS THIS STAGE (for downstream reference): (1) src/app/api/webhooks/mercadopago/route.test.ts (NEW, 20) — the webhook ROUTE trust boundary was previously only exercised live-by-hand; now automated with real crypto + real verifyWebhookSignature: signature gate (missing/tampered/malformed/no-secret → 401 no processing), C-1 route-seam lock (query-id-valid passes, body-id-valid REJECTED 401), M-5 body cap (Content-Length 413 + lying-length streamed 413), edge-12 type dispatch (non-payment 200, no-data-id 200, non-JSON no crash), httpOk→HTTP mapping (advance-blocked/mp-unavailable → 500, thrown error → 500 no-leak), data.id sourcing (query preferred, body fallback, action threaded). (2) src/components/checkout/payment-panel.test.tsx (NEW, 15) — every DB state + client overlay + a11y role (unpaid/paid/paid+refunded/failed/processing/pending-voucher; unavailable/error/not-payable overlays; redirect on success; retry same token). (3) src/components/checkout/oxxo-spei-instructions.test.tsx (NEW, 12) — voucher defensive degradation (all fields missing → no crash/no Invalid Date/no undefined), SPEI CLABE swap, amber-not-green, locale expiry es-MX≠en, clipboard feature-detect. (4) src/lib/payments/money-boundary.test.ts (+4) — dense 0..2000¢ round-trip incl .x5 boundaries, large totals, string↔number agreement, half-centavo guard. (5) tests/integration/payments.integration.test.ts (+3) — advance_order_status full legal forward chain + illegal backward matrix + legal skip-jump. === QA FORWARD FACTS FOR UX (Stage 8): the PaymentPanel/OxxoSpei render branches + a11y roles are locked by component tests — role=alert on failed/unavailable, role=status on paid/processing/voucher, aria-busy on the pay region, sr-only polite live regions for redirect/copied announcements, amber-not-green voucher, no-NaN totals, no horizontal overflow at 375px (e2e). data-testids are stable (see dev-done.md "Data-Testids Added"). i18n keys are checkout.payment.* symmetric both locales (keys-used test passes). UX must not remove/rename testids or roles without updating these tests. HUMAN-REVIEW GATE + BLOCKED-ON-USER live sandbox both remain OPEN regardless of the SHIP verdict. === Stage 6 (ultrafix) COMPLETE — all 2 CRITICAL + 8 MAJOR FIXED; MINORs 3/5 fixed (m-3/m-6 skipped w/ reason); NITs 4/5 fixed (N-3 documented). tasks/review-findings.md has the full per-finding resolution log; tasks/dev-done.md has the Stage-6 tracker + forward facts. VERIFICATION: tsc 0 errors; eslint clean; next build clean (webhook route ƒ /api/webhooks/mercadopago; tsconfig restored); unit 1126/1126 (baseline 1107 + 19 new regression); integration 151/151 (baseline 144 + 7 new) via scripts/run-integration.sh; migration 0009 applies clean on supabase db reset (idempotent re-key). DB left pristine-seeded; no stray servers. === FORWARD FACTS FOR QA (Stage 7) — the reworked semantics to test: (a) DEDUPE SPINE re-keyed unique(mp_payment_id, mp_status) via record_payment_event RPC (claim-then-finalize): finalized same-(id,status) replay → duplicate no-op; status PROGRESSION (OXXO/SPEI pending→approved) → distinct claim → processed (AC-18 now PASS); UNFINALIZED claim (crash between claim & advance) → reclaimable/reprocessed. finalize_payment_event sets processed_at only AFTER a successful advance (M-6). (b) TRANSITION MATRIX: approved→paid; pending/in_process→pending; authorized→authorized; rejected/cancelled→failed (order stays pending_payment); refunded→PAYMENT-ONLY (advance_order_status p_order_status=null → sets payment_status=refunded + writes history, order_status untouched — works on paid AND shipped, C-2); charged_back/in_mediation/unknown→flag (no advance). (c) RPC RESULT: callers branch on result.reason — advanced/payment_updated/noop_same_status = success; regression_blocked/order_not_found = advance-blocked (500, claim left unfinalized so MP retry converges, M-7). advance_order_status now returns extra reason 'payment_updated'; noop branch writes history iff payment_status changed (m-1). (d) REPLAY WINDOW: WEBHOOK_REPLAY_TOLERANCE_MS=5min in webhook.ts — ts parsed (seconds/ms) and checked AFTER the HMAC; stale/future/unparseable → 401 (reason stale_timestamp/unparseable_ts). AC-8 now PASS: verifier fed ONLY the query-string data.id (C-1), body id is fetch-only. (e) BODY LIMIT: MAX_WEBHOOK_BODY_BYTES=64KB in route.ts — content-length over cap → 413 before read; bounded stream read caps actual bytes (M-5). (f) REFUNDS: new payment_refunds ledger (unique mp_refund_id) + record_refund RPC (locks order, sums prior, rejects prior+amount>total race-safely, M-2) + refunded_total read helper; every full AND partial refund writes a durable ledger row at success (M-3); full refund advances via payment-only mode. refund.ts pre-checks remaining balance; a full(null) refund = remaining balance after priors. (g) M-8: config resolvePaymentMethod — 'atm' payment_type unmapped (→null unless oxxo/clabe/spei method id); method_id is primary signal. (h) N-5: mp_payment_events.raw column DROPPED. New RPCs in database.types.ts as TYPE ALIASES (not interfaces — same Supabase footgun): RecordPaymentEventArgs, FinalizePaymentEventArgs, RecordRefundArgs/Result, RefundedTotalArgs; AdvanceOrderStatusArgs.p_order_status now OrderStatus|null. QA: mock MP everywhere (no real creds); live-sandbox still BLOCKED-ON-USER; re-run signed-synthetic-webhook exercise (valid→processed; bad/missing sig→401; replay→idempotent; OXXO pending→approved→paid; refunded→refunded; amount mismatch→flagged; stale ts→401). === Stage 5 (Review) was: REQUEST CHANGES, score 6.5/10 (tasks/review-findings.md). 2 CRITICAL, 8 MAJOR, 6 MINOR, 5 NIT. Stage 6 (ultrafix) MUST fix C-1/C-2/M-1 at minimum then remaining MAJORs. CRITICAL FINDINGS FOR FIX: (C-1) webhook signature manifest uses body data.id as fallback — MP signs with the QUERY-STRING data.id only; body fallback → false 401s → real payments never reconcile (route.ts:46-49 + webhook.ts:78-92). (C-2) mapMpStatus("refunded") hard-codes orderStatus:'paid' → on advanced orders the regression guard blocks it so payment_status never becomes refunded, and on paid orders the noop branch writes no history (payments-status.ts:101-110 + process-payment.ts:137-145). (M-1, near-critical) mp_payment_events unique(mp_payment_id) drops every later status change for a payment id → breaks OXXO/SPEI pending→approved (AC-18) and refund/chargeback webhooks; re-key per (payment_id,status) or notification id (0009_payments.sql:40-55 + process-payment.ts:93-107). Other MAJORs: M-2 no cumulative over-refund guard (edge 9, refund.ts:71-77); M-3 partial refund leaves no durable audit (refund.ts:157-165); M-4 no ts replay-window check (webhook.ts); M-5 unbounded body DoS (route.ts:41); M-6 claim/advance not atomic → transient failure = permanent stuck order (process-payment.ts:94-150); M-7 callers ignore RPC result.reason so regression_blocked reads as success (advance-order.ts + callers); M-8 payment_type 'atm'→'spei' miscategorization drives wrong voucher UX (config.ts:117-147). AC-8, AC-18 marked FAIL; AC-10/13/14/19/23 PARTIAL. HUMAN-REVIEW GATE STILL OPEN regardless of verdict. === Prior Stage 4 (Dev) COMPLETE — full T8 Mercado Pago Checkout Pro implementation shipped (tasks/dev-done.md). VERIFICATION: tsc 0 errors; eslint clean; next build clean both locales (webhook route = ƒ /api/webhooks/mercadopago); unit 1107/1107 (baseline 924 + 183 new); integration 144/144 (baseline 137 + 7 new — RPC advance/idempotency/regression + mp_payment_events unique/cascade); e2e 8/8 on PRODUCTION build (chromium+mobile, both locales — pay-now panel, pay-now→unavailable edge-11, EN copy, no mobile overflow); migration 0009 applies clean on db reset+seed; webhook exercised LIVE with signed synthetic payloads (valid sig gated, bad sig 401, missing sig 401, non-payment 200 ignore). DB left pristine-seeded; tsconfig.json clean; .env.local gitignored; no stray servers. KEY IMPLEMENTATION FACTS FOR REVIEW: (1) advance_order_status RPC (0009) is the ONLY status-transition path — SECURITY DEFINER, empty search_path, service_role-only, writes order_status_history, idempotent no-op on same status (no dup history), regression-guarded via order_status_rank helper (paid never regresses to pending). (2) mp_payment_events unique(mp_payment_id) idempotency spine + explicit grant to service_role (0005's blanket grant is point-in-time, doesn't cover a table created in 0009 — this bit us, now fixed). (3) Webhook trust boundary (src/lib/payments/webhook.ts pure + route.ts): x-signature HMAC-SHA256 verified with timingSafeEqual BEFORE any side effect; fail-closed 401; data.id lowercased in manifest; ts verbatim. On payment: authoritative Payment.get (never trust body), mp_payment_events dedupe claim (23505→duplicate no-op), order match by mp_external_reference OR confirmation_token, ZERO-tolerance amount reconciliation gates ONLY the paid transition, then advance via RPC. 200 for processed/dup/unknown/mismatch/flag (MP stops retrying), 500 only for internal/MP-down. (4) Money boundary money-boundary.ts: exact integer/string cents↔MP-decimal, no float drift, round-trip tested. (5) Refund refund.ts: guards paid; full→refunded via RPC, partial→stays paid (documented AC-19 rule, no history row on no-op); per-request idempotency key; typed results, never echoes raw MP error. (6) Secrets: getMercadoPagoEnv() server-only (access token + webhook secret); mp-client.ts import "server-only"; NO NEXT_PUBLIC_ MP secret; public key intentionally unused (redirect surface). secret-exposure.test.ts asserts static discipline. (7) UI: <PaymentPanel> (5 DB-derived states + client unavailable/error overlays), <OxxoSpeiInstructions> (defensive voucher, useSyncExternalStore for copy feature-detect — no setState-in-effect), hero branches paid vs received, truth-from-DB (?mp_status is display hint only via derivePanelState). i18n checkout.payment.* both locales symmetric; stale confirmation.title/noPaymentTitle/noPaymentYet REMOVED; keys-used.test updated. IMPORTANT TS GOTCHA (do not "fix" back): AdvanceOrderStatusArgs/Result are `type` aliases NOT interfaces — Supabase's GenericFunction.Args extends Record<string,unknown> rejects interfaces → whole Database type collapses to never. dep added: mercadopago@^3.2.0. REVIEW FOCUS (payment/trust-boundary — human review still mandatory before merge): signature verification, amount reconciliation, refund execution, RPC transition/idempotency, secret handling. === Stage 3 (UI Design) COMPLETE — tasks/ui-design.md has the T8 payment UI spec (truth-from-DB, <PaymentPanel> replaces "Sin pago todavía" block, <OxxoSpeiInstructions> amber-not-green defensive voucher, hero paid/received split, no new motion CSS, no new shadcn primitive). === Stage 1+2 (PlanResearch) COMPLETE. Complexity=HIGH → full-cycle runs ALL 12 stages (incl. Hacker); Security+Arch at full depth. Feature Type=full-feature. Chosen MP surface: Checkout Pro (redirect/preference) — only product confirmed to cover card+OXXO+SPEI+wallet in Mexico, SAQ-A, composes with token-addressed confirmation page. Ticket+research written (tasks/next-ticket.md, tasks/research-report.md). BINDING DESIGN INPUTS honored: R-1 advance_order_status RPC (writes order_status_history), R-3 mp_payment_events unique(mp_payment_id) idempotency spine separate from orders.idempotency_key, R-4 indexes on mp_payment_id/mp_external_reference. KEY FACTS: orders table ALREADY has mp_preference_id/mp_payment_id/mp_external_reference/payment_method cols (nullable, left mutable by immutability trigger); payment_status enum pending|authorized|paid|failed|refunded + order_status already cover T8 (NO new enum values). .env.local has PLACEHOLDER MERCADOPAGO_* vars (correct names). GATES: (1) HUMAN-REVIEW mandatory before merge regardless of verdict — payment code; (2) LIVE-SANDBOX verification BLOCKED-ON-USER — all tests mock MP; OXXO/SPEI approval cannot be simulated in test (use signed synthetic webhooks). CAVEAT for dev: voucher field paths ambiguous — use transaction_details.* (not point_of_interaction), verify vs real sandbox. T7 record: verdict SHIP; human-review gate still OPEN.

=== STANDING GATES ===
- HUMAN-REVIEW GATE (BUILD_PLAN rule 3): payment code (T8) ALWAYS flagged for human review before merge regardless of any SHIP verdict. Same gate is still OPEN for T7 (verdict SHIP, but T7 remains UNCHECKED in BUILD_PLAN.md until the user approves; do not check it off).
- T7 human-review focus list (from ship-decision.md): 0008_checkout.sql create_order SECURITY DEFINER fn; checkout server-action trust boundary (actions.ts); rate-limit.ts best-effort caveat + CHECKOUT_RATE_LIMIT_DISABLED unset in real deploys; money math (order.ts, discount.ts, cart/shipping.ts).

=== ENV CONSTRAINT (checked 2026-07-14) ===
- NO Mercado Pago credentials in .env.local (only Supabase vars). T8 must: wire ALL MP config via env vars (never hardcode; no secret ever NEXT_PUBLIC_), mock the MP API in unit/integration tests, and mark live-sandbox verification BLOCKED-ON-USER (document exact env var names + where to get sandbox creds in dev-done.md). Do not fabricate credentials.

=== T8 DESIGN INPUTS (binding, from T7 Arch review, tasks/architecture-review.md) ===
- R-1: order status transitions MUST go through a new advance_order_status Postgres RPC that also writes order_status_history — never ad-hoc .update({status}). Immutability trigger on orders leaves status/payment_status/mp_* mutable by design.
- R-3: payment idempotency is a SEPARATE spine from orders.idempotency_key (which only covers creation retry). Duplicate MP webhooks / webhook-during-retry need a unique mp_payment_id guard.
- R-4: index mp_payment_id / mp_external_reference in T8's migration (webhook filters by them; currently unindexed).
- TD-1 (cheap, optional): test asserting TS ORDER_NUMBER_PREFIX == RPC 'PP-' literal. TD-2: distributed rate limiter is a pre-launch follow-up. TD-3: RPC errors are classified by string-match (improve if touched).

=== T7 FACTS T8 BUILDS ON ===
- Orders land status=pending_payment, payment_status=pending via create_order RPC (0008_checkout.sql), invoked from the checkout "use server" action with createAdminClient() (RLS denies anon writes to commerce tables; RPC granted to service_role only).
- Confirmation page addressed by unguessable orders.confirmation_token (uuid) at /[locale]/checkout/confirmacion/[token]; order_number (PP-000001…) is display-only and 404s as a URL. confirmationPath(token) in config.
- Integer cents everywhere; DB CHECKs (0003_commerce.sql): total=subtotal+shipping+tax-discount, discount<=subtotal, line_total=unit_price*qty, currency='MXN'. Immutability trigger freezes the financial snapshot after insert (status/payment_status/mp_* stay mutable).
- Per-IP rate limiter on placeOrder (5/min, src/lib/checkout/rate-limit.ts), CHECKOUT_RATE_LIMIT_DISABLED=1 server-only bypass wired into playwright.config.ts.
- BUILD_PLAN T8 scope: card, OXXO, SPEI, MP wallet via sandbox creds from env; pending-payment state for OXXO/SPEI with instructions; webhook endpoint with signature verification + idempotent handling to confirm payments and advance orders; card-decline retry flow; refund execution API used by admin (T12). CRITICAL: payment code requires human review before merge.

=== E2E/ENV INFRA (binding) ===
- LOCAL Docker Supabase only; remote empty/unlinked — apply new migrations locally, never remote push.
- Authoritative e2e: PRODUCTION build (next build + next start), separate NEXT_QA_DIST_DIR, CHECKOUT_RATE_LIMIT_DISABLED=1. E2e that places orders depletes seed stock — reseed before/after; leave DB pristine. After any prod build with NEXT_QA_DIST_DIR: `git checkout -- tsconfig.json` (next build auto-injects dist-dir type globs — leaked once in T7, cleaned by Verify).
- Pre-existing T3/T4 "resolved to 2 elements" PDP flake under full-suite parallel load is documented and NOT a new-task failure.
- Test baseline after T7: unit 924/924, integration 137/137, checkout e2e 24/24, cart e2e 46/46.
