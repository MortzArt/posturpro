# Pipeline State
Task: T11 — Admin: product management
Tier: full-cycle (high) — ALL 12 stages incl. Hacker (Stage 11)
Stage: 4
Agent: ultradev
Last Updated: 2026-07-15
Notes: T10 SHIPPED (see summary below) and checked off in BUILD_PLAN.md. User is AFK — run T11 autonomously end-to-end.
=== S3 (ultradesign) COMPLETE (commit c5b7e41) — tasks/ui-design.md organized by 7 slices. DECISIONS: nested sidebar sub-nav under "Catálogo" group (Productos/Taxonomía/Preguntas; Q&A gets unanswered-count badge); CSV = 2 buttons in product-list header, not nav; tabs ONLY in taxonomy manager (4 entity types). Product list: mobile CARD COLLAPSE (not h-scroll), tablet h-scroll; status badges dot-shape+text never color-only; filters in URL params; mobile filters reuse .drawer-panel. Product form: single long form + sticky scroll-spy rail (NOT tabs), one save, focus-first-invalid, unsaved-changes alert-dialog + beforeunload; images/variants/Q&A = sections on edit page. Images: native Pointer Events drag + mandatory ↑/↓ keyboard buttons, NO @dnd-kit; handle-only, setPointerCapture, useOptimistic, interruptible settle; cover = radiogroup. Variants: inline-editable rows saved with form. Taxonomy: recursive CategoryTree role=tree arrow-keys; re-parent via edit-dialog select (drag = enhancement); cycle prevention client + check_violation net; delete-restrict friendly msg. Inventory: dialog explicit product-vs-variant target, required reason, live resulting-stock preview negative-block. CSV: 4-step stepper (Seleccionar→Vista previa dry-run→Confirmar→Resultado), per-row es-MX error taxonomy, zero writes until confirm. NEW SHADCN: alert-dialog + progress (beyond table/textarea/dialog/tabs). Hand-rolled: drag/dropzone/tree/CSV/stepper — zero new runtime deps. Only 2 new CSS classes (.dialog-content-motion, .reorder-item), RM-guarded. Motion appendix M1–M15 + icon appendix in spec.
=== S1+2 COMPLETE (commit 4110eb0) — Complexity HIGH (all 12 stages), Feature Type FULL-FEATURE. SCOPE: all 9 sub-features KEPT (CSV import/export = Phase 1 per PRODUCT_SPEC ×3, zero new deps hand-rolled RFC-4180, ships LAST behind mandatory dry-run preview; Q&A answering = small slice, product_questions.answer/is_published/answered_at exist, storefront reads published — NO asker emailing, no email column, don't add). DEFERRED per rule 2: discount-code UI, rich-text editor (plain textarea), translation editing, bulk actions. IMAGE STORAGE DECISION: re-enable [storage] in supabase/config.toml (keep [analytics]/[edge_runtime] OFF — those caused the boot regression, not storage); next.config already allow-lists /storage/v1/object/public/**; MUST verify clean stop&&start&&db reset before ship (AC-2/edge 10); filesystem fallback documented behind image-write.ts interface. NEW: migration 0011 (inventory ledger + atomic RPC), pagination/indexed-filter read convention (T10 arch req). BUILD = 7 SLICES: foundation → list → product form → images → variants → taxonomy → inventory/duplicate/Q&A → CSV; Slice 0 blocks rest. KEY RISKS: storage re-enable vs db reset; clean-code caps under 9 features (up-front module decomposition); CSV catalog corruption (dry-run + per-row strict parse + taxonomy-by-slug); admin list must use BASE TABLE + admin client + NO cache (products_public/cachedRead hides drafts/serves stale — explicit AC); /api/admin/* bypasses middleware guard — writes stay in server actions or self-guard. CACHE TAGS to bust on admin writes (verified): catalog + touched brand:<slug>/style:<slug>/category:<slug>/product:<slug> via ONE shared helper importing CATALOG_CACHE_TAG/productCacheTag, never string literals. FILE MAP: src/lib/admin/products/{list-query,list-filters,product-read}.ts; src/lib/admin/{products,taxonomy,inventory,qa,csv}/* (paired *-input.ts pure parser + *-write.ts) + cache-tags.ts, slug.ts, units.ts; actions+form-state under src/app/admin/(app)/products|taxonomy|qa|import-export/; ~20 components under src/components/admin/products|taxonomy/*; shadcn table/textarea/dialog/tabs vendored via CLI; nav flip in constants.ts; regenerate database.types.ts.

=== T10 COMPLETE — SHIP (Stage 12 verdict, quality 9/10, confidence HIGH, 2026-07-15) ===
- Pipeline: full-cycle medium (hacker skipped). S5 review APPROVE-WITH-FIXES 8.5/10 → S6 all majors fixed; S7 QA PASS HIGH; S8 UX 9.5/10; S9 SECURE (0 crit/high); S10 arch APPROVE 9/10 T11-READY.
- Final verified numbers (Stage 12, independent): tsc 0, eslint clean, prod build exit 0, unit 1376/1376 (78 files), integration 188/188 (14 files), e2e storefront regression 78/78 prod (payment 8 + checkout 24 + cart 46 × chromium+mobile), admin unauth-guard 6/6 prod, admin authed 30/30 dev-server serial. AC 16/16, edges 10/10. Migrations still 0001..0010 (T10 added none).
- WHAT EXISTS NOW (T11 builds on this): self-managed HMAC-SHA256 HttpOnly session-cookie admin auth (NOT Supabase Auth). src/lib/admin/{constants,session-payload,session,session-edge,auth,login-rate-limit,settings-input,session-guard}.ts; locale-free /admin tree: root layout + (app) route-group guard sub-layout + login/ + settings via actions.ts; components/admin/{admin-shell,admin-nav,logout-button,admin-page,login-form,store-settings-form}. Defense-in-depth: middleware /admin branch (Edge crypto.subtle verify, returns BEFORE next-intl) + (app) layout node:crypto verify + per-action requireSession(). Admin is es-MX only, no next-intl provider. updateStoreSettings() in store-settings.ts = the write template (admin client + updateTag cache bust).
- T11 EXTENSION SEAMS (arch-verified): new admin sections = flag-flip in src/lib/admin/constants.ts AdminNavItem[] (status "soon"→"live" + href) + new route dirs under src/app/admin/(app)/ — they inherit guard+shell automatically. Follow updateStoreSettings pattern for writes; sliding-window rate-limit core is shared (3 call sites) — reuse, don't copy.
- T11 ARCH REQUIREMENT: add a pagination + indexed-filter convention for product lists (the settings singleton maybeSingle read pattern does NOT generalize).
- ENV (dev, .env.local, gitignored): ADMIN_EMAIL=admin@posturpro.mx, dev password posturpro-dev-2026, ADMIN_PASSWORD_HASH (scrypt — every $ MUST be backslash-escaped in .env* files, QA P1: dotenv $-expansion silently corrupts it; dev-only assertAdminPasswordHashFormat guard fails fast; hash-gen snippet in T10 dev-done.md emits pre-escaped), ADMIN_SESSION_SECRET, optional ADMIN_SESSION_MAX_AGE_SECONDS (default 8h), ADMIN_LOGIN_RATE_LIMIT_DISABLED=1 (e2e escape hatch).
- ADMIN E2E HARNESS RULES (from Stage 12 root-cause): run e2e/admin.spec.ts SERIALLY (--workers=1) on a FRESH dev server + fresh seed — settings tests mutate the shared store_settings singleton (parallel-worker race) and mid-run reseeds go stale in the dev route cache. Authed admin e2e CANNOT run against next start (NODE_ENV=production → Secure cookie rejected over plain HTTP — correct behavior); unauth-guard tests DO run on prod build.

=== T12 GATES (from T10 security+arch — MUST land before/during T12, not needed for T11) ===
- SEC-M-1 / ADR-2: stateless sessions have NO server-side revocation (stolen cookie valid ≤8h; only lever is rotating ADMIN_SESSION_SECRET). Acceptable for T11 product edits; NOT for refund-capable T12 sessions — add session-version check (payload v field reserved) or shorten max-age before T12.
- Middleware matcher EXCLUDES /api → any /api/admin/* route handler (T12 refunds/uploads?) is NOT middleware-guarded; each must call the session check at entry. Documented admin API-route rule.
- Security residuals accepted (documented in security-audit.md): SEC-M-2 per-IP limiter IP-rotation bypass (scrypt cost + password entropy is the real defense); SEC-M-3 ADMIN_SESSION_MAX_AGE_SECONDS Edge/Node drift (fail-safe, Node authoritative); LOW: client-bundle secret scan should gate on prod build in CI.

=== STANDING GATES (carry forward — do not drop) ===
- HUMAN-REVIEW GATE (BUILD_PLAN rule 3): T7 and T8 both have advisory SHIP verdicts but remain UNCHECKED in BUILD_PLAN.md until the user manually reviews. Do not check them off.
- T7 human-review focus: 0008_checkout.sql create_order SECURITY DEFINER fn; checkout server-action trust boundary (actions.ts); rate-limit.ts best-effort caveat + CHECKOUT_RATE_LIMIT_DISABLED unset in real deploys; money math (order.ts, discount.ts, cart/shipping.ts).
- T8 human-review focus (from ship-decision.md): webhook signature verification, amount reconciliation, refund execution, RPC transition/idempotency, secret handling.
- T8 LIVE-SANDBOX verification BLOCKED-ON-USER (only placeholder MERCADOPAGO_* keys in .env.local; all tests mock MP).
- T9 rule-3 addendum: T9 modified the checkout action (src/app/[locale]/checkout/actions.ts — locale persistence + email triggers) and src/lib/payments/process-payment.ts (transition_kind email dispatch) — include these diffs (bdd37bc, 6c19265) in the same human review pass as T7/T8.
- Clean-code A4 addendum (2026-07-14): A4 refactor commit also touches the flagged checkout files (form-parsing.ts extraction + canonical client-ip.ts) — include in the human-review pass; verified by checkout e2e 24/24 prod.
- T9 LIVE-SEND verification BLOCKED-ON-USER: no EMAIL_* vars in .env.local; all tests mock the provider. Env vars in T9 dev-done: EMAIL_API_KEY (Resend), EMAIL_FROM_ADDRESS, EMAIL_OWNER_ADDRESS, NEXT_PUBLIC_SITE_ORIGIN; dev EMAIL_DEV_PREVIEW=1.
- ADVISORY (not rule-3, T10): user may want to eyeball the admin auth core when reviewing T7/T8 — src/lib/admin/{session,session-edge,auth}.ts + middleware /admin branch — it passed dedicated review+security stages (0 crit/high) but it IS the admin trust boundary.

=== T9 SEAMS FOR LATER TASKS ===
- T12 (admin): shipped / cancelled / refund_issued templates + send functions exist, tested, NOT live-wired — wire from admin actions. refund.ts still called nowhere until T12 auth-gates it.
- T13 (contact page): contact_relay template + send function exist, tested, NOT live-wired — wire from the contact form.
- Emails branch on order_status_history.transition_kind (RPC-returned) — never string-match note text.

=== T8 FACTS LATER TASKS BUILD ON ===
- advance_order_status RPC (0009) is the ONLY status-transition path; writes order_status_history; payment-only mode (p_order_status=null) sets payment_status=refunded with order_status untouched.
- Webhook processing: record_payment_event claim-then-finalize spine keyed (mp_payment_id, mp_status); callers branch on result.reason (advanced/payment_updated/noop_same_status = success).
- payment_refunds ledger + record_refund RPC exist; refund.ts is server-only, called nowhere until T12.
- Confirmation page addressed by orders.confirmation_token at /[locale]/checkout/confirmacion/[token]; confirmationPath(token) in config.
- Integer cents everywhere; currency MXN; storefront i18n es-MX + en symmetric (admin is es-MX only).

=== E2E/ENV INFRA (binding) ===
- LOCAL Docker Supabase only; remote empty/unlinked — apply new migrations locally, never remote push. Migrations 0001..0010; next is 0011.
- Authoritative e2e: PRODUCTION build (next build + next start), separate NEXT_QA_DIST_DIR, CHECKOUT_RATE_LIMIT_DISABLED=1 (+ ADMIN_LOGIN_RATE_LIMIT_DISABLED=1 for admin). Reseed between order-placing suites; leave DB pristine. After any prod build with NEXT_QA_DIST_DIR: `git checkout -- tsconfig.json`.
- SEQUENCING RULE: on PROD builds, ALWAYS `supabase db reset` + seed BEFORE `next build`; after ANY reseed, REBUILD (stale dist-dir cache serves old variant UUIDs → add-to-cart stores dead variant id → order-placing e2e fails stuck at /checkout with 0 orders). Dev servers render per-request. Do NOT mistake this signature for a Server-Action/FormData bug.
- KNOWN FLAKES (pre-existing, non-blocking): cross-project mobile stock-depletion race in combined all-projects runs (run per-project with reseed-between); payment-panel unit flake (passes isolated); 2 moderate postcss advisories (transitive via next).
- Test baseline after T10: unit 1376/1376 (78 files), integration 188/188 (14 files), e2e payment 8/8 + checkout 24/24 + cart 46/46 + admin 30/30 (serial dev) + admin-guard 6/6 (prod).
- caffeinate is running from the interactive session — leave it alone. No dev server currently running (port 3000 clear).
