# Pipeline State
Task: T7 — Checkout & order creation
Tier: full-cycle
Stage: 8 (UX COMPLETE)
Agent: ultrasecurity + ultraarch (Stages 9+10 — run IN PARALLEL) next
Complexity: high (classified stage 1)
Feature Type: full-stack
Last Updated: 2026-07-14
Notes:
- Stage 8 (UX) done: tasks/ux-audit.md written. Score 9.5/10. Findings 3 (0 critical, 1 major, 2 polish); ALL 3 fixed, 0 deferred. The n-1 carried-forward gap (global-error banner had NO recovery action) is CLOSED: GlobalBanner now renders a status-appropriate recovery action — `error`/`shipping-unavailable` → "Reintentar"/"Try again" type="submit" (re-runs placeOrder, disabled while pending); `price-changed`/`out-of-stock` → "Volver al carrito"/"Back to cart" Link to CART_PATH (a naive retry would just re-fail, so send to cart). Banner is now `<div role="alert">` (was `<p>`); data-testid="checkout-banner" preserved (e2e lock intact), action is data-testid="checkout-banner-action". New i18n keys checkout.banner.retry + checkout.banner.review added to BOTH locales (symmetric). Polish: wired the previously-DEAD checkout.discount.savings key into the applied pill ("Ahorras {amount}" replaces bare −$X — kills dead copy, states benefit; e2e $679.90 assertion still holds; testid checkout-discount-savings). Mobile summary accordion = allowed deviation (sticky bar satisfies UX-Requirements "collapsible top OR sticky bar"), documented not changed. Files changed (6): checkout-flow-client.tsx (+GlobalBanner recovery, exported GlobalBanner+BannerRecovery for tests), use-checkout-labels.tsx (banner.retry/review), discount-code-field.tsx (savings text), checkout-flow-client.test.tsx (+3 banner tests), es-MX.json + en.json (banner keys). VERIFIED: tsc clean, eslint clean, next build clean (both locales); unit 918/918 (was 915 → +3), integration 135/135, checkout e2e 24/24 on PROD build (NEXT_QA_DIST_DIR=.next-t7-ux, chromium+mobile). No regression-locked behavior changed. QA build dir removed; DB reseeded PRISTINE (0 orders, 70 variants, 5 discount codes, stock restored). NO new motion CSS (design mandate honored — reused existing globals.css classes only). tsconfig.json build-artifact edit reverted (kept clean).
- Stage 7 (QA) done: tasks/qa-report.md written. VERDICT: SHIP (QA perspective), Confidence HIGH — subject to the HUMAN-REVIEW GATE (do NOT auto-merge). 153 new tests, all green: 104 unit/component (checkout total 151) + 25 integration (checkout-rpc 11 + checkout-read 12 + seed +2) + 24 e2e (checkout.spec 12×chromium/mobile). Full suites: unit 915/915 (was 811 → +104, 46 files), integration 135/135 (11 files), checkout e2e 24/24 (run twice, deterministic). Every AC (16/16) + every edge case (8/8) has ≥1 passing test. Stage-6 fixes regression-locked: M-1 distinct errorId+aria-describedby, M-2 one-submit-per-breakpoint, M-3 live-region amount, M-4 focus-first-invalid incl. state trigger, M-5 notes label, M-6 IDOR (getOrderByToken null for malformed/seq-number/unknown; live 404 for PP-000001 + bad token), m-1 applyLivePrices, m-2 RPC re-assert is_active+window, m-3 upper(code) unique, m-7 inputMode=tel. RPC verified LIVE: happy(customer+order+items+history, stock−qty, sales+qty, confirmation_token uuid, pending/pending, tax=0, MXN), idempotency(same order/reused/no double-decrement), OOS full rollback, MULTI-LINE rollback, LAST-UNIT RACE(1 wins/1 rolls back, stock floor 0), DISCOUNT_EXHAUSTED for exhausted+expired, total-identity CHECK backstop, anon execute denied. tsc clean, eslint clean.
- Bugs found: 2 TEST bugs fixed (stale seed count 69→70/99→100 in seed.integration.test.ts; count() helper union missing discount_codes). ZERO production code bugs.
- E2E INFRA used: prod build NEXT_QA_DIST_DIR=.next-t7-qa (separate from dev .next), next start -p 3000, local Docker keys. QA build dir REMOVED; DB reset+seeded to pristine (0 orders/customers, Milano Negro stock=8, AHORRA10 redeemed=0). Pre-existing T3/T4 "resolved to 2 elements" PDP-spec flake still present under parallel load (NOT T7) — checkout.spec has 0 failures always.
- FORWARD FACT for Verify (Stage 12): e2e places REAL orders that deplete finite seed stock (~8/run on the Negro default variant). RESEED (or supabase db reset + seed) before an authoritative e2e run, else gotoPDP add-to-cart disables (toBeEnabled timeout). Two consecutive runs on a stock-restored DB were both 24/24 green.
- FORWARD FACT for UX (Stage 8): global-error banner still has no retry/"review your cart" recovery action (dead banner.retry removed in Stage 6) — Stage 8 to design/wire (carried from Stage 6). Discount UX is validate-on-submit (no async pre-check). State picker is Radix Select (accessible combobox, allowed deviation). noValidate is intentional (server-action boundary).
- Stage 6 (Fix) done: all 6 MAJORs FIXED (M-1 distinct errorId `${id}-error`; M-2 in-card submit `hidden lg:flex`; M-3 live-region interpolates `formatMXN(discountCents)`; M-4 DOM-order first-invalid focus incl. StateField `triggerRef`; M-5 notes `<label>`+`notes.label` i18n; M-6 opaque `confirmation_token` — column+RPC+token-routed page, IDOR CLOSED). Minors: m-1 (applyLivePrices refresh on price-changed), m-2 (RPC re-asserts is_active+window), m-3 (unique index upper(code)), m-7 (inputMode="tel") FIXED; m-4/m-5/m-6 SKIPPED (justified deviations); n-1 dead i18n keys removed. VERIFIED: tsc clean, eslint clean, next build clean (both locales, route now `/[locale]/checkout/confirmacion/[token]`), vitest 811/811. Migration 0008 amended (local Docker only, idempotent) + reseeded; RPC smoke happy/idempotent/out-of-stock + discount active/expired/exhausted all pass; live prod-server: valid token 200, bad token 404, sequential PP-000003 404. Smoke data cleaned, DB left seeded. review-findings.md + dev-done.md updated.
- Stage 5 (Review) done: tasks/review-findings.md written. Verdict APPROVE-WITH-FIXES, score 8/10. 0 CRITICAL, 6 MAJOR, 7 MINOR, 7 NIT. Write path (RPC atomicity/rollback/idempotency/snapshot-revalidation/discount-clamp/DB-CHECK/admin-grant) verified correct; no SQLi, no priv-esc, no $NaN, no float. Defects concentrated in client a11y + one responsive bug + one PII exposure. ultrafix (Stage 6) must fix all 6 MAJORs: M-1 duplicate id/broken aria-describedby across ALL text fields (errorId == input id; fix to `${id}-error`); M-2 in-card submit missing `hidden lg:flex` → two live submit buttons below lg (add to checkout-summary.tsx:158); M-3 live-region "discount applied" interpolates amount:"" (pass formatMXN(discountCents)); M-4 focus-first-invalid wired for email only (StateField also takes no ref); M-5 delivery_notes textarea has no <label>; M-6 sequential order_number → enumerable PII on confirmation (coordinate with Security stage — opaque-token). Also address m-1 (price-drift totals do not refresh to live price — edge 1 partial) and consider m-2 discount TOCTOU.
- Stage 1+2 (PlanResearch) done: tasks/next-ticket.md + tasks/research-report.md written.
- Stage 3 (UI Design) done: tasks/ui-design.md written (T7 checkout — overwrote the leftover T6 spec).
- Stage 4 (Dev) done: tasks/dev-done.md written. All 16 ACs implemented, 8 edge cases handled. 27 files created/modified. Migration 0008_checkout.sql (atomic create_order RPC) applied to LOCAL Docker Supabase + seed re-run (zero-stock variant + 5 discount codes). VERIFIED: tsc clean, eslint clean, next build clean (both locales), npm test 811 passed (764 existing + 47 new). RPC smoke-tested in DB container (happy path PP-000001, idempotency reused:true no double-decrement, OUT_OF_STOCK full rollback, DISCOUNT_EXHAUSTED). Dev-server smoke: /checkout 200 ES+EN, unknown confirmation 404, real confirmation renders. Smoke data cleaned up.
- FOR REVIEW (Stage 5): discount = validate-on-submit (option B, allowed by design); idempotency = client UUID + partial-unique index on orders.idempotency_key; ORDER_NUMBER_PREFIX "PP" duplicated in RPC by design; confirmation reads by guessable order_number (flag for Security, opaque-token follow-up); no placeOrder rate limit (deferred, best-effort follow-up).
- Complexity = HIGH → full-cycle runs ALL 12 stages (do not skip hacker).
- Feature Type = full-stack → every stage at full depth (UI Design + UX both run).
- HUMAN-REVIEW GATE (BUILD_PLAN rule 3): checkout ALWAYS flagged for human review before merge regardless of any SHIP verdict. Verify stage must surface this and NOT auto-merge. Do NOT check [x] T7 in BUILD_PLAN until the user approves.
- UI DESIGN KEY DECISIONS (for dev, Stage 4): single-page sectioned form (NOT wizard); one <form> + one placeOrder action (Q&A useActionState precedent). Reuse cart OrderSummary math verbatim (computeShipping/totalCents/formatMXN). Reuse existing globals.css motion classes ONLY (.enter-fade/.stagger/.price-value/.cart-press/.cart-step-press/.select-content-motion/.grid-pending) — NO new motion CSS. Fields = Q&A `fieldClasses` raw inputs bumped to min-h-11 + raw <label text-sm> (NOT shadcn Input/Label, for parity). State picker = vendored shadcn Select (h-11 w-full trigger) + hidden input for FormData. NO Textarea/Card/Alert/Skeleton ui components exist — use raw <textarea>+fieldClasses, card=rounded-lg border bg-card p-4/5, alert=<p role=alert .enter-fade text-destructive>, skeleton=inline animate-pulse. Mobile: collapsible top summary + sticky translucent bottom bar (Total+submit); in-card submit hidden lg:flex to keep one submit per form. CheckoutFormState union defined in ui-design.md (sibling checkout-form-state.ts per "use server" rule). New i18n `checkout` namespace fully drafted in ui-design.md (both es-MX.json default + en.json). Confirmation route /checkout/confirmacion/[orderNumber]; OrderConfirmation client child clears cart once on mount. Discount field states: idle/checking/applied/invalid(5 reasons)/degraded — never blocks submit (AC-7). shipping-unavailable → submit disabled, never $NaN. Flag for Security: confirmation reads order by guessable order_number (opaque-token follow-up, out of scope T7).

=== CRITICAL FORWARD FACTS FOR DOWNSTREAM STAGES ===
- WRITE PATH: orders/order_items/customers/discount_codes are FULLY denied to anon in RLS (0005 lines 252-258). Checkout MUST use createAdminClient() (server-only, secret key). No app/api route handlers exist — use a "use server" action (Q&A precedent: producto/[slug]/actions.ts + qa-form-state.ts).
- ATOMICITY: orders+order_items+order_status_history insert AND per-line stock decrement AND sales_count bump MUST be ONE Postgres function (new migration 0008_checkout.sql), invoked via admin.rpc(). Guarded decrement: UPDATE ... SET stock=stock-qty WHERE id=$id AND stock>=qty RETURNING id; zero rows → raise exception → rollback. Handles last-unit race (edge 2).
- DB CHECKS checkout must satisfy exactly (0003_commerce.sql): total=subtotal+shipping+tax-discount; discount<=subtotal; line_total=unit_price*qty; currency='MXN'; order_number NOT NULL UNIQUE; all *_cents>=0. Immutability trigger freezes the snapshot after insert.
- SNAPSHOT NOT AUTHORITATIVE: re-read live product/variant by id (new src/lib/checkout/checkout-read.ts — no getProductById exists yet, only slug-based getProduct). Recompute unit price = variant.price_override_cents ?? product.price_cents. IGNORE cart snapshot price/qty (edge 4).
- effectiveStock() in catalog/stock.ts SUMS variant stock — it is DISPLAY-ONLY. Reservation decrements the SPECIFIC variant (or product row for no-variant lines). Do NOT reserve against the summed value.
- REUSE shipping math (src/lib/cart/shipping.ts: computeShipping/totalCents), formatMXN (money.ts), getStoreSettingsStatic() (page fetch, carrito/page.tsx pattern), useCart() + hydrated gate (cart-provider.tsx).
- store_settings unavailable → computeShipping returns "unavailable" → BLOCK submit (edge 5), never sell with shipping=0.
- Discount FIELD in scope, management UI is Phase 2 (out of scope). Bad code degrades to full price, never blocks (AC-7).
- Tax=0 Phase 1, written to tax_cents/tax_base_cents columns (CFDI Phase 3 needs no rework). RFC optional captured/stored.
- Order lands pending_payment / payment_status pending. NO payment capture (T8).
- SEED additions needed: one zero-stock variant (live oversell e2e) + discount codes (active pct, active fixed, expired, below-min, exhausted) in scripts/seed-data/products.ts + scripts/seed.ts.
- CONFIG additions (0-tunable rule 4): ORDER_NUMBER_PREFIX+format, MEXICAN_CP_PATTERN, MEXICAN_STATES (32), CHECKOUT_CONFIRMATION_PATH/confirmationPath(n), TAX_RATE=0. CHECKOUT_PATH already exists.
- i18n: add "checkout" namespace to BOTH es-MX.json and en.json (Spanish default). No new npm deps (hand-rolled validation, Q&A precedent).
- ENV: LOCAL Docker Supabase only; remote empty/unlinked — apply 0008 locally, never remote push. Run authoritative e2e against production build (next build+start), separate NEXT_QA_DIST_DIR (T6 QA infra note).
- CONFIRMATION-TOKEN CONTRACT (Stage 6 M-6 fix — for QA/e2e): the confirmation page is addressed by an UNGUESSABLE `orders.confirmation_token` (uuid, `not null default gen_random_uuid()`, unique index), NOT the sequential `order_number`. Route is `/[locale]/checkout/confirmacion/[token]` (folder renamed from `[orderNumber]`). `create_order` RPC + `CreateOrderResult` now return `confirmation_token: string`; success `CheckoutFormState.confirmationToken` drives the redirect; `getOrderByToken(token)` (was `getOrderByNumber`) validates UUID shape then looks up by `confirmation_token`. E2E ASSERTIONS: after placing an order the URL is `…/confirmacion/<uuid>` (a uuid, not `PP-…`); a malformed/unknown token → 404; the sequential `PP-000001…N` value → 404 (NOT a valid entry point). The order number `#PP-…` is still DISPLAYED on the page. `confirmationPath()` in config now takes the token.
- i18n DELTA (Stage 6): added `checkout.notes.label` (both locales). REMOVED dead keys: `checkout.discount.apply`, `checkout.discount.checking`, `checkout.banner.dismiss`, `checkout.banner.retry`, `checkout.summary.itemsCount`. Locales remain symmetric.
- A11y/RESPONSIVE DELTA (Stage 6): every checkout field error id is `checkout-<field>-error` (distinct from input id); in-card submit is `hidden lg:flex` (below lg ONLY the sticky-bar `checkout-submit-sticky` is active; at lg+ ONLY the in-card `checkout-submit`) — e2e mobile submit must target `checkout-submit-sticky`; focus moves to the first invalid field in DOM order (incl. the state Select trigger); notes textarea now has a `<label>`.
- UX-STAGE FOLLOW-UP: [RESOLVED Stage 8] global-error banner now HAS a status-appropriate recovery action (retry submit for error/shipping-unavailable; "back to cart" Link for price-changed/out-of-stock). banner.retry + banner.review re-added to both locales. data-testid checkout-banner-action.
